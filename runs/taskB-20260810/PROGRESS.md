# 작업 B 배치 2026-08-10 (시작 16:18 KST)

## 매트릭스와 예상
| 단계 | 내용 | 런수 | 예상 |
|---|---|---|---|
| 0 | S2 조성 M0/M1/M2 × 2점 (w 공유 판정) | 6 | 16:18–17:10 |
| 1 | 용량 제약 구현 + selftest + 회귀 (런 0) | 0 | 16:20–17:40 |
| 2 | 검증① 1600k×800: far+on×3, far+off×2, strict+off×1 교대 | 6 | ~18:40 |
| 3 | 검증② 트리거 캘리브 2~3 + strict off/on·far on 각 3 | 11~12 | ~21:00 |
| 보고 | TASKB_REPORT.md + 클린 복원 | – | ~21:45 |

## 사전 등록 (실행 전 고정)

**검증② 판정 기준 3종** (§5.3 그대로): ① cap-on 락인 0/n ② blocked_by=capacity(또는 both의
capacity 성분)가 S2 이동을 막은 tick 존재 ③ 진동 지표(전환 수·주기-2 구조) 소멸.

**★해석 예측 2건 (구현 전 기록 — 실측과 대조할 것)**:
1. 검증①에서 cap-on은 기대(6~9%)에 못 미칠 가능성이 높다. 근거: 1600k 밴드에서 search의
   S2 slack = 45−5−15−4.3−24.6 = −3.9 < 0 → 용량 항은 집합에서 사이트를 "빼는" 것만 가능하고
   slack 음수인 S2로 "이월"은 불가(§3.1 설계 그대로) → 양 코호트 search는 여전히 S1 EXPECTANT
   266rps → 위반 ≈70% 유지 예상. 이 경우 규명 보고: 용량 항은 과적재 차단이지 손실 최소화
   이월이 아니다 — 이월에는 별도 설계(soft assignment) 필요.
2. 검증②에서 strict+on의 위험 모드: S3 관측 요동 tick에 S2·S1이 용량 차단되면 EXPECTANT
   S1(1400 전량)로 떨어짐 → S3↔S1 왕복 가능성. 락인 여부는 S3 복귀 시 콜드스타트 과도에 달림.
   발생하면 기준 ①③ 실패로 기록하고 "용량 항 + 체류 선호(또는 최소피해 폴백) 필요"가 발견이 됨.

**단계0 판정 기준**: w 공유 = S1 w 고정으로 S2 적합 시 잔차 ≤ 2×(과제A train 0.50); C(S2) 브래킷
일관성(3조성이 단일 C로 설명). 불성립/판정불가 → 정지·보고 (§2.3).

## 런 로그 (append)
### 16:18 단계0 기동 (6런, S2 M0 900/1200 · M1 570/750 · M2 1250/1650, 32conn).
### 16:5x ★내 실수: s0 배치 실행 중 sorts.yaml.tmpl 편집 → 구 러너가 새 토큰(%CAPACITY_CHECK%) 미치환
  → 뒤 4런 SKIPPED (렌더 검증이 설계대로 막음 — 피해는 시간 손실 ~25분뿐). 새 러너로 --resume 재개.
  교훈: 배치 실행 중 렌더 소스(tmpl)·배포 대상 파일 편집 금지 (러너 코드는 기동 시 로드라 안전).
### 16:52 단계1 구현 완료: obs 도착률 관측(XFF×클래스, 배제 전 카운트), decide_live(용량 필터+blocked_by),
  DEC 9열 추가, tmpl 용량 블록(+출처 주석), run_all 렌더/스파이크. selftest 8케이스 통과(off/r_eq=0 회귀 포함).
### 16:51 단계0 완료 6/6 DONE(재개 2패스), SUSPECT 0. ★§2.3 분기 = 공유 성립:
- C-only 적합 잔차 0.218 ≤ 1.0(기준) / 브래킷 교집합 [474,536] / C(S2)=515 CI[495,530] (파생 520 CI 안).
- S2 실측: M0 900→27.5(3.5%)/1200→붕괴, M1 570→35.9(13%)/750→붕괴(달성 664), M2 1250→17.6/1650→붕괴.
- 자유 w 보조검정은 west C격자(≤340)가 S2 범위 밖이라 미산출 — 주 판정 성립으로 불필요, 기록만.
- c_eq[S2]=515 tmpl 갱신. → 단계 1(완료됨) → 검증 ① 진행.
### 17:0x 검증① 기동 (v1, 6런: far on/off/on/strict/off/on).
### 17:44 검증① 완료 6/6, SUSPECT 0. ★사전 예측 1 적중 (v1_results.json):
- far+on 74.21±1.97 ≈ far+off 74.91 (기대 6~9% 미달성). strict+off 95.03(야간과 일치).
- 기전 규명: on 런 search 유닛 108틱 전부 blocked_by=slack — 관측 f_c로 S1 slack도 음수 →
  EXPECTANT 직행, 용량 분기 미발동. 실패 모드 = "갈 곳 없음"(전 사이트 slack<0)이라 용량 항 관할 밖.
  이월(6~9%)에는 손실 최소화 배정(soft assignment)이 필요 — §3.1 의미론 밖, 다음 결정 사항으로.
- set_share 근사 오차: {S2,S3} 예측 0.58 vs 실측 0.580~0.582 (<0.2%p) — (b) 예측 기반 설계 유효.
- 드리프트 기록: far+off 오늘 74.9 vs 야간 70.5 (+4.4%p) — 같은 배치 내 비교만 사용.
### 17:45 v2cal 기동 (트리거 캘리브 2런, strict+off+spike).
### 18:01 v2cal 완료 2/2 락인 (70.6%/63.9%, 전환 2520/1980) — 스파이크(+400rps 4s @t+30)가
  락인을 결정적으로 유발 확인 (자연 발생률 3/7 대비). 주입 조정 불필요. 본배치로.
### 18:02 검증② 본배치 기동 (8런: soff_3, son_1, faron_1, son_2, faron_2, son_3, faron_3).
### 19:03 검증② 완료 7/7 (+캘리브 2), SUSPECT 0. ★사전 등록 기준 3종 전부 통과 (v2_results.json):
- strict+off 락인 3/3 (70.4/70.6/63.9%) → strict+on 락인 0/3 (0.415/0.423/0.456%) → far+on 0.13~0.15%.
- 기준②: strict+on capacity/both 6~7틱, S2 차단 3~4틱 — S2 실과부하 국면(락인 연료) 차단 실증.
- 기준③: 전환 27~31 (락인 1980~2520 대비), 주기-2 소멸. 우려했던 S3↔S1 왕복 미발생(EXPECTANT 이탈 1~2틱 후 안착).
- 스파이크 중 용량 초과 시 S3도 일시 차단(853eq>749) — 'capacity' 틱의 일부. far+on도 S2 1~2틱 차단 작동.
### 19:10 sorts.yaml 기본 렌더(capacity off) 재배포 md5 일치. 클린 복원 완료(잔재 0, 컨테이너 72 무재시작, 디스크 173GB).
### 19:15 TASKB_REPORT.md 확정. 배치 종료 — 21/21 DONE, SUSPECT/FAILED 0.
