# 작업 B3 보고 — 밴드 의존 유효 용량 · 검증 ① 재재실행 (2026-08-10 23:02 ~ 08-11 01:30)

배치 4개(s0 10런 + s0b 5런 + s0c 3런 + v1c 6런) 총 24런 전부 DONE,
**SUSPECT/FAILED/SKIPPED 0**. 클린 복원 검증 완료 (기본 렌더 재배포 md5
일치, 잔재 0, 컨테이너 72 재시작 0, 디스크 171 GB). 진행 로그
`runs/taskB3-20260810/PROGRESS.md`, 분석 `analysis/taskB3/`. 정의·형태·판정
기준·해석 예측은 전부 실행 전 PROGRESS.md 에 사전 등록돼 있다.

## 1. 한 줄 결론 (단계별)

- **단계 0 — C_eff(S1, band)**: 3점 확정 — 무제한 206.1 / 2300 k **161.7**
  (−21.5 %) / 1600 k **105.4** (−48.9 %). 밴드가 유효 용량을 절반까지
  깎는 것을 정량화. B2 역산 추정(210~220)은 과대였다 (§2).
- **단계 1 — 구현**: (T) 실측 테이블 + 최근접·extrapolated 규칙 구현,
  selftest(기존 전체 + ceff 8케이스) 통과, **회귀 96행 동일** (off = B2
  결정 단위 동일) — §3.3 정지점 통과.
- **단계 2 — 검증 ① 재재실행**: **C_eff on 6.50 ± 0.85 %** vs off(B2 재현)
  30.59 ± 2.85 % — **한 자릿수 도달, 채움 진동 소멸 (교대율 1.0 → 0.00),
  B2 on_1 편차(25 %p) 소멸 (0.4~0.7 %p)**. 기준 ①③ 통과, 기준 ②는
  진동 부지표 2종 압도 통과 + 회피-비율 부지표만 경계 미충족 (지표 설계
  결함 — §4 정직 기록).

**엣지 경합 창의 전체 여정: strict 95~98 % → far_tier 74.6 % → soft
assignment 28.1 % → C_eff 6.5 %.** 야간 §4 의 "6~9 %" 추정이 두 번의
설계 결정(손실 배분 + 밴드 의존 용량)을 거쳐 실측이 됐다.

## 2. C_eff(S1, band) — 3점, 하락률, B2 추정 대조 (`ceff_fit.json`)

사전 등록 정의(§A): max-클래스 위반율 ≤ 1 % 최대 등가 도착률, 로그-선형
보간, CI = 교차 괄호 2점. 조건 = M0·16 conn/코호트·양 코호트 상시 밴드
(검증 조건 정합, `standing_band_both` 러너 확장).

| band | C_eff [eq] | 브래킷 | 무제한 대비 |
|---|---|---|---|
| 무제한 | 206.1 | [203.9, 222.9] | – |
| 2300 k | 161.7 | [151.8, 180.2] | −21.5 % |
| 1600 k | 105.4 | [94.8, 118.6] | **−48.9 %** |

- **지시 §0 의 기전이 실측으로 섰다**: 같은 서버가 밴드만 좁아져도 SLO-유효
  용량이 절반이 된다 — d_acc(지연 축)와 별개의 용량 축.
- **B2 역산 추정(210~220 eq)과의 대조: 과대였다.** 그 추정은 f_c p95 예산
  근사(단독 곡선 보간)였고, 1 % 위반 기준 실측은 105 — corrected 는 f_c
  외에 커넥션 HOL·스케줄 밀림을 포함하므로 예산 근사보다 훨씬 일찍 1 % 를
  넘는다. (예측 E-1 의 값 부분은 어긋남 — 절차대로 실측 우선. 진동 소멸
  이라는 방향 부분은 적중, §4.)
- 스윕은 하향 2회 확장으로 브래킷 완성 (정의 §A 불변, 범위만 확장 —
  1600 k 는 최저 eq 75.9 까지 내려 0.86 % 확인). 정지점 미발동.
- **HEADROOM 재확인 (§3.2)**: 파국 무릎(위반 10 %→63 %)은 1600 k 에서
  eq 223~237, 2300 k 에서 223~247 — 1 % 경계(105/162)보다 훨씬 위라
  0.9 는 보수적. **밴드별 여유율 분리 불요**, 0.9 유지.
- 무제한 SLO-유효 실측 206.1 은 **C(붕괴-적합) 279 와 다르다** — 사전 등록
  §B 대로 결정식의 무제한 항은 c_eq 유지(§3.3 회귀 요구 정합), 이 괴리는
  §10-2 결정 사항으로 올린다.

## 3. 등록한 C_eff 형태와 근거 (§C)

**(T) 실측 테이블** (권고 그대로): `c_eff_band[site][band_kbit]`,
측정 밴드(1600/2300) 정확 일치 시 그 값, 불일치 시 **가장 가까운 측정점
값 + `c_eff_extrapolated=1` + 경고** (조용한 외삽 금지 — selftest 로 검증),
무제한 → c_eq (§B), 미측정 사이트(S2/S3) → 밴드 무관 c_eq (한계, §9).
C_eff 선택 기준 밴드 = 결정 유닛의 관측 밴드. soft room 도 동일 C_eff.
decisions 3열 추가 (c_eff_s1 / band_used / c_eff_extrapolated) — 검증 런
실측: 밴드 창 105.4 / 무제한 창 279 정확 전환, **extrapolated 0행**.

## 4. 검증 ① 재재실행 — 사전 등록 기준 3종 판정 (`v1c_results.json`)

| arm | both 창 위반율 | 평균±반범위 |
|---|---|---|
| far+cap+soft+**C_eff on** (n=3) | 6.896 / 7.148 / 5.453 | **6.50 ± 0.85** |
| far+cap+soft+C_eff off (n=2) | 27.738 / 33.435 | 30.59 ± 2.85 (B2 28.06 재현) |
| strict+off (n=1, 참조) | 97.996 | – |

1. **유의 개선 — 통과**: 차 24.1 %p > 임계 3.7 %p. **한 자릿수 도달** —
   §4.1 의 성공 요건을 원 조항으로 충족.
2. **채움 진동 소멸 — 부지표 (i)(ii) 통과, (iii) 경계 미충족 (정직 기록)**:
   - (i) 0↔채움 교대율 **전 런·전 유닛 0.00** < 0.3 (B2 0.96~1.00) — 통과
   - (ii) 인접 |ΔS1 가중치| **0.0~0.4 %p** < 30 (B2 90.7~93.8) — 통과
   - (iii) S1 완전 회피 비율 0.330/0.333/0.333, 임계 "< 1/3" — **2/3런
     경계 실패**. 원인: 이 지표는 B2 의 진동성 회피(S2:100 이 교대로 등장)
     를 겨냥해 등록했는데, C_eff 후의 상태는 **결정적 분업** — c1 유닛이
     S1 room(94.9 eq)을 매 tick 선점·지속 채움(duty 0.00)하고 c2 유닛은
     room 잔여가 없어 항상 S2 전담(duty 1.00, 교대율 0.00). soft 틱 수가
     c1:c2 ≈ 2:1 이라 전체 회피 비율이 구조적으로 1/3 에 수렴한다.
     **회피가 아니라 안정 배정이므로 진동 소멸의 반증이 아니다** — 그러나
     사전 등록 문구 기준으로는 미충족이 맞고, 그대로 기록한다 (기준을
     결과 보고 바꾸지 않음).
3. **이월 목적지 편차 — 통과 (전 런)**: 0.4 / 0.3(0.7) / 0.4 %p ≤ 6 %p.

**종합: 성공 판정** — ①③ 통과 + 한 자릿수 도달(①의 원 조항), 진동은
직접 지표(교대율·진폭)로 소멸 입증. ②-(iii) 은 지표 설계 결함으로 문구상
미충족을 명기한다.

**방법론 절차 기록 (지시 §서두)**: B2 예측 D-1("room 251 > 밴드 유효 용량
→ 채움↔회피 진동") → B3 실측(C_eff(1600)=105 확정, room 을 94.9 로 내리자
진동 소멸·6.5 %) — **구현 전 PROGRESS.md 사전 등록 → 실측 대조 → 어긋난
값 부분(210~220 과대)까지 기록**하는 절차를 반복 사용했다. 이번에도 예측
E-1(방향 적중·값 어긋남)·E-2(잔여 주성분 = S2 이월 search, 적중 — §5)·
E-3(무제한 SLO-유효 < C, 적중)을 같은 절차로 남겼다.

## 5. 남은 위반의 구성 분해 (6.5 %)

| 셀 | rps | viol | 총위반 기여 |
|---|---|---|---|
| **S2 이월 search** | ~169 | 22.2~29.6 % | **4.7~6.3 pp (86~88 %)** |
| S3 recommend | ~150 | 3.2~4.0 % | 0.6~0.7 pp |
| S1 search | ~97 | 0.5~0.8 % | 0.1 pp |
| 그 외 (reserve·S2 recommend) | – | ≤0.3 % | ≤0.1 pp |

- 잔여의 사실상 전부가 **S2 이월 search** — fc95 7.1~7.4 로 서버는 건강,
  위반은 slack −3.9 ms 구조 경계(1600 k 에서 S2 의 SLO 예산 부족) 탓.
  예측 E-2 적중. 이것은 배정으로 못 없앤다 — 어디에 둬도 위반하는
  트래픽의 자리 선택 문제이고, 남은 개선 여지는 SLO/밴드 자체의 축이다.
- S1 search 는 완전 회복 (fc95 45.3 → 4.8, viol 51 % → 0.7 %).
- off 재현 런은 B2 진동을 그대로 재현 (c2 교대율 0.96~1.00) — 대조 성립.

## 6. on_1 편차 소멸 여부 (지시 §1-4)

**소멸.** B2 on_1 의 의도-실측 25 %p 편차 → B3 전 런 0.3~0.7 %p.
off(B2 재현) 런조차 1.2/2.6 %p — B2 on_1 은 진동 위상 잡음이 맞았다는
방증이 성립한다 (재현 조건에서 재발하지 않았으므로 단정은 아님, 1런 사건).

## 7. 내가 판단해서 결정한 것과 근거

1. **스윕 하향 확장 2회** (s0b/s0c) — 1 % 교차가 초기 범위 아래였다.
   정의(§A)는 불변, 측정 범위만 확장 — "브래킷 전혀 안 잡힘" 정지점은
   확장으로도 못 잡을 때를 위해 남겼고 발동하지 않았다.
2. **1600 k 저부하의 비단조 잡음 처리**: eq 75.9→0.86 %, 94.8→0.77 %,
   118.6→1.39 % (±0.3 %p 잡음). 교차 보간은 등록 절차대로 인접 2점
   (94.8~118.6)으로 했고 CI 가 그 폭을 보고한다.
3. **②-(iii) 를 문구 그대로 실패로 기록** — 지표의 취지(진동)와 실측
   상태(분업)가 어긋난 것은 지표 설계 결함이지 판정 완화 사유가 아니다.
   종합 판정은 ① 의 원 조항(한 자릿수 + 유의 개선)으로 성립시켰다.
4. **미측정 고밴드(예: 20000)의 최근접 함정을 주석·경고로 방어** —
   지시 §3.1 의 최근접 규칙을 따르되, 20000→2300 사상이 위험함을 tmpl
   주석 + extrapolated 플래그로 드러나게 했다 (이번 실험에선 무제한/1600
   만 발생, extrapolated 0행 실측).

## 8. 실패·SKIP·SUSPECT

**24/24 DONE, SUSPECT/FAILED/SKIPPED 0.** 가드 위반 0. 정지점 발동 0.

## 9. 검증하지 못한 채 남긴 것

- **S2/S3 의 밴드-유효 용량 미측정** (지시 범위가 S1) — S2 이월 search 가
  잔여의 주성분인 만큼, S2 의 1600 k 유효 용량이 c_eq 515 보다 낮다면
  현재 room 이 과대일 수 있다 (이번 조건에선 S2 적재 ~330 eq 로 여유 커서
  미발현 추정 — 실측은 없음).
- **무제한 SLO-유효(206) vs C(279) 괴리의 결정식 반영 여부** (§B 로 유보).
- C_eff 는 M0 조성 조건부 (조성 축은 이번에 고정 — w 공유 문제와 동일한
  볼록포 한계가 밴드 축에도 적용될 수 있음).
- 미측정 밴드의 최근접 규칙은 이번 실험 조건에서 미발동 — 20000 함정은
  주석·플래그 방어만 있고 실측 검증 없음.
- **S3 w 공유 불성립의 기전 가설 (지시 §1-3, 한 줄, 미검증)**: S3 는
  cpuset 4코어로 가장 커서 병목이 CPU 가 아닌 다른 자원(메모리 대역폭,
  mongo I/O)으로 옮겨갔을 가능성 — 소형 요청(reserve)이 CPU-비례 w 보다
  싸게 처리되는 것과 부합한다. 추측이며 검증하지 않는다.
- 드리프트 판정 실험은 지시대로 코드 동결 후로 유보 (이번 배치도 열/주파수
  계측은 계속 수집 — thermal.json 24런분 축적).

## 10. 다음에 내가 결정해야 할 것

1. **코드 동결 선언 시점** — 검증 ① 이 6.5 % 로 닫혔으니 B3 코드를 동결
   기준으로 삼을지. 동결 후 야간 드리프트 판정 실험 → 작업 I 순서(§1-2)가
   걸려 있다.
2. **무제한 SLO-유효(206) vs c_eq(279) 괴리** — 결정식의 무제한 room 이
   0.9×279=251 로, 1 % 기준으로는 과대다. 무제한 창에서 EXPECTANT 가 뜨는
   조건이 오면 같은 채움 문제가 재발할 수 있다 (이번 조건에선 미발현).
   c_eq 를 SLO-유효로 교체할지 — B2 회귀 경계가 깨지므로 별도 결정 필요.
3. **S2 밴드-유효 용량 측정 여부** — 잔여 6.5 % 의 주성분(S2 이월 search)
   의 room 정확성에 걸리나, 현 조건에선 여유가 커 우선순위 낮음.
4. **논문 구성**: 엣지 경합 절의 서사가 완성됐다 — strict 95→far 74.6→
   손실 배분 28.1→밴드 의존 용량 6.5 (각 단계 사전 등록·예측·실측 대조
   포함). I-14 별도 절(§1-4) + 용량 모형 절(밴드 축·유효 범위)로 나눌지.
