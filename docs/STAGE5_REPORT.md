# 5단계 보고 — 부하 스윕: 우위가 생기고 사라지는 경계 (G2 제출, 2026-08-12 22:5x)

원자료 `runs/stage5-20260812/`(**23/23 DONE**, FAILED/SUSPECT 0, 무인 중단 0)
· 프로브 `runs/stage5-probe-20260812/`(8런) · 스모크 `runs/stage5-smoke-20260812/`
· 분석 `analysis/stage5/`(s5_analyze · probe_analyze · stage5_driver) · 사전 등록
`analysis/stage5/PREREG_S5.md`(확정본) · G1 경위 `analysis/stage5/G1_REPORT.md`.

**조건**: 엣지 경합(`seq_extreme` 1600 kbit, c1@120 → 둘 다@180 → 해제@240),
2 코호트, T = 1 s, 관측 shim off(ogstun 직결), primitive v4, **hc_off**
(G1 §1.2 — 앵커 taskB3 와 같은 config 계열, 동결 접촉 0). **노드 장애 축은
이 스윕의 대상이 아니다** — HC 는 그 축의 장치이고 여기서 조작한 변수는
부하뿐이다(HC 무영향은 1단계 브리지 런 6.132 % ∈ 6.50 ± 0.85 로 확인됨).

## 0. 한 줄 결론

**격차는 역U 가 아니라 역U 를 지나 부호가 뒤집힌다.** 무릎 아래에서 ≈0
(0.055 %p), 무릎 근처·위에서 최대(+4.27 %p @ L = 800), **극단 과부하에서
−15.9 %p — SORTS 가 최선 비교군보다 나쁘다.** 오른팔이 "닫힌다"가 아니라
"넘어간다"는 것이 이 단계의 새 사실이고, 기전은 §4 에 있다.

## 1. 정책 × 부하 (both 창 = 양 코호트 극단, 주 지표)

| L | 정책 | n | **위반%** | ± | S1/S2/S3 분배 | S1 도달 eq | **K비** | 달성% | 밀림 p50 | c1only% |
|---|---|---|---|---|---|---|---|---|---|---|
| 200 | **SORTS** | 2 | **0.171** | 0.038 | .333/.372/.294 | 66.6 | 0.63 | 100.0 | 0.2 ms | 0.054 |
| 200 | bl_loc_pri | 1 | 0.226 | – | 1.00/0/0 | 94.8 | 0.90 | 100.0 | 0.2 ms | 0.216 |
| 200 | bl_lr | 2 | 8.373 | 1.150 | .463/.301/.236 | 44.2 | 0.42 | 100.0 | 0.2 ms | 4.217 |
| 450 | **SORTS** | 2 | **0.420** | 0.119 | .216/.495/.288 | 97.3 | 0.92 | 100.0 | 0.1 ms | 0.100 |
| 450 | bl_loc_pri | 2 | 4.162 | 0.419 | 1.00/0/0 | 213.4 | 2.02 | 100.0 | 0.1 ms | 2.281 |
| 450 | bl_lr | 2 | 7.407 | 0.117 | .506/.280/.214 | 107.8 | 1.02 | 100.0 | 0.1 ms | 3.574 |
| 800 | **SORTS** | 2 | **6.340** | 0.218 | .121/.598/.281 | 97.0 | 0.92 | 100.0 | 0.1 ms | 1.759 |
| 800 | bl_loc_pri | 1 | 100.000 | 0.000 | 1.00/0/0 | 379.4 | 3.60 | **74.3** | **65.3 s** | 100.0 |
| 800 | bl_lr | 2 | 10.614 | 0.153 | .490/.295/.215 | 186.2 | 1.77 | 100.0 | 0.1 ms | 5.276 |
| 1400 | **SORTS** | 2 | **46.779** | 2.292 | .069/.379/.552 | 96.4 | 0.92 | 100.0 | 0.1 ms | 12.933 |
| 1400 | bl_loc_pri | 1 | 100.000 | 0.000 | 1.00/0/0 | 663.7 | 6.30 | **46.4** | **143.7 s** | 100.0 |
| 1400 | bl_lr | 2 | 30.855 | 0.139 | .371/.364/.266 | 246.1 | 2.33 | 100.0 | 0.1 ms | 16.197 |

K비 = S1 도달 수요[eq] / **K = C_eff(S1 | 1600 k) = 105.4 eq**(PREREG §1.2).
런 요약의 `s1_knee_ratio`(상수 400 기준)도 원자료에 남아 있으나 **그 상수는
판정에 쓰이지 않는다** — 무릎으로 읽지 말 것(`run_all.py:71`, 보고 전용).

**SORTS 제어 지표**(both 창, `s5_ctl_metrics.json`): 반응(주입 지시 기준)
0.47~1.41 s(tick 위상), subset 전환(플립) **L=200 0 → 450 16~17 → 800
24~31 → 1400 245~264**. S1 유효 사용 duty 0.333(200) → 0.167(800·1400).

## 2. 격차 곡선 (`s5_gap_curve.csv` — 총 부하 축 + K비 축 동시 방출)

| L | K비(lr) | K비(loc_pri) | SORTS | bl_lr | bl_loc_pri | 최선 비교군 | **격차 %p** |
|---|---|---|---|---|---|---|---|
| 200 | 0.42 | 0.90 | 0.171 | 8.373 | 0.226 | bl_loc_pri | **+0.055** |
| 450 | 1.02 | 2.02 | 0.420 | 7.407 | 4.162 | bl_loc_pri | **+3.742** |
| 800 | 1.77 | 3.60 | 6.340 | 10.614 | 100.000 | bl_lr | **+4.274** |
| 1400 | 2.33 | 6.30 | 46.779 | 30.855 | 100.000 | bl_lr | **−15.924** |

CSV 열: `L_total_rps, k_ratio_bl_lr, k_ratio_bl_loc_pri, k_ratio_sorts,
k_ratio_best_comparator, viol_*, best_comparator, gap_pp`.

## 3. 4단계 누적표(6.50 %)는 곡선의 어디인가 — 이 단계의 최종 값어치

**L = 800, K비(lr) 1.77 — 격차가 최대인 지점**이다. 같은 조건 재현 실측
**6.34 ± 0.22 %**(`s5_sorts_L800_1/2` = 6.494 / 6.186)로 taskB3 앵커
**6.50 ± 0.85 %** 안에 정확히 들어온다(hc_off, 16 conn, 동일 판정창).

즉 지금까지의 결과 전체(strict 95 → far 74.6 → soft 28.1 → C_eff 6.5)는
**곡선의 우위 최대점 한 점에서 측정된 값**이다. 그 왼쪽(무릎 아래)에서는
어떤 정책이든 문제가 없고, 그 오른쪽 끝에서는 **부호가 뒤집힌다.**

## 4. 왜 1400 에서 뒤집히는가 (기전, 사이트×클래스 실측)

`s5_sorts_L1400_1` vs `s5_lr_L1400_1`, both 창 rps / 위반% / f_c p95 / slack:

| | SORTS | bl_lr |
|---|---|---|
| S1_search | 97.7 / **1.81 %** / 6.8 ms / +31.2 | 173.3 / 83.6 % / 48.0 ms / **−10.0** |
| S2_search | 184.3 / **99.18 %** / 40.8 ms / **−15.8** | 169.8 / 45.9 % / 9.1 ms / +15.9 |
| S3_search | 184.6 / 100 % / 5.7 ms / +9.3 | 123.7 / 100 % / 3.9 ms / +11.1 |
| S3_recommend | 364.0 / **57.1 %** | 165.5 / 34.1 % |
| 사이트 총부하 | S1 98 / S2 546 / S3 756 | S1 520 / S2 508 / S3 372 |

1. **용량 게이트가 S1 을 지킨다 — 너무 잘 지킨다.** SORTS 는 S1 을
   K비 0.92(=C_eff 이내)로 고정해 S1 위반을 1.8 %로 묶었지만, 그 대가로
   S1 에 **1400 중 98 rps(7 %)** 만 실었다. 나머지가 S2/S3 로 몰려 S2 는
   slack −15.8 ms 로 붕괴(99.2 %)한다. bl_lr 은 S1 에 520 rps 를 실어
   S1 을 83.6 % 위반시키지만 **총합은 더 낮다**(30.9 vs 46.8).
2. **원인은 비대칭 용량 모형**이다. `c_eff_band` 테이블은 **S1 만 실측**
   되어 있고 S2/S3 는 밴드 무관 `c_eq` 로 대체된다(taskB3 §3 (T)·§9 한계로
   이미 등록됨). 컨트롤러는 S2/S3 를 실제보다 크게 보고 계속 밀어 넣는다.
3. **진동이 겹친다**: subset 전환이 800 의 24~31 에서 1400 에서 **245~264**
   로 급증하고, 결정이 S2↔S3 를 왕복한다(`W:S2:100` 28 / `W:S3:100` 27 /
   `site_s3` 70 틱). I-9/I-13 계열 되먹임 진동의 고부하 발현이다.
4. 따라서 **"우위 상실"은 접근의 원리적 한계가 아니라 배치된 용량 테이블의
   비대칭**에서 온다 — 다만 그것이 이번 조건의 실측 사실이고, 이 구간을
   "전부 붕괴해 격차가 닫힌다"로 서술하면 **틀린다**(닫히는 게 아니라
   역전한다).

## 5. 사전 등록 판정

| 등록 | 결과 |
|---|---|
| **P-S5-1** 역U | **성립(내부 점 최대)** — 최대 +4.274 %p @ L = 800(K비 1.77), 양 끝점(200 · 1400)에서 축소. 단 **오른쪽 끝은 축소가 아니라 부호 반전(−15.9 %p)** 이므로 "역U" 보다 **"열렸다 닫히고 뒤집힌다"** 가 정확하다(§4) |
| **P-S5-2** 무릎 아래 무해 | **통과** — L = 200 격차 **+0.055 %p** ≥ −0.1. SORTS 0.171 % vs 최선 비교군(loc_pri) 0.226 % |
| **P-S5-3** locality 붕괴 시점 | **통과** — 10 % 교차 부하: **bl_loc_pri 546** < bl_lr 742 < SORTS 937. loc_pri 는 자기 K비 1.0 교차(L ≈ 222) 직후부터 열화(0.226 → 4.162 → 100 %) |
| **P-S5-4** §4.2 정합 | **별도 절 §6** |
| **P-S5-5′** 생성 건전성 | 23 런 중 **21 런 달성 ≥ 99.9 %**(밀림 0.1~0.2 ms, 오류 0). 미달 2 런은 전부 **갈래 (b) 시스템 용량 초과**: `s5_loc_L800_1` 74.3 %(svc p50 36 ms, 밀림 65.3 s), `s5_loc_L1400_1` 46.4 %(svc p50 68.9 ms, 밀림 143.7 s) → **결과로 채택**(달성 병기). 갈래 (a) 생성기 구성 부족 **0 런** |

**커넥션 대조**(800 × 32 conn, G1 §1.3 승인): bl_lr 8.812 %(16 conn 10.614,
**Δ −1.802**), SORTS 5.634 %(16 conn 6.340, **Δ −0.706**). 커넥션을 늘리면
양 정책 다 좋아지며 lr 이 1.1 %p 더 이득이다. 따라서 1400 점(32 conn)의
격차 −15.9 %p 는 16 conn 환산 시 **≈ −14.8 %p** 로 **부호·크기 결론 불변**.

## 6. P-S5-4 — 등록 산식과 기전이 어긋난다 (판단 요청)

등록 산식: "격차가 처음 0.5 %p 를 넘는 L 에서의 **K비(bl_lr)**".
실측: 격차 0.5 %p 교차 **L ≈ 331** → **K비(bl_lr) = 0.73 → 갈래 A**.

그러나 **그 지점에서 격차를 만드는 비교군은 bl_lr 이 아니라 bl_loc_pri** 다
(200·450 에서 최선 비교군 = loc_pri). 짝을 맞추면 **K비(loc_pri) = 1.49 →
갈래 B**. 등록 산식이 "최선 비교군 기준 격차"와 "bl_lr 기준 K비"를 **다른
정책에 걸어둔 것**이 원인이고, 산식대로의 0.73 은 **아무 정책의 용량 상태도
가리키지 않는 값**이다.

- 갈래 A(원 문구 유지)는 §4.2 에 유리한 결론이므로, 산식이 기전과 어긋난
  것을 알고도 A 를 취하는 것은 지시 §2 의 "유리한 해석 금지"에 어긋난다.
- **권고: 갈래 B 적용** — 변별 개시 K비 **1.49**(범위 1.2~2.0). 등록 문구:
  > 우위는 용량 무릎 자체가 아니라 **무릎을 일정 배율 초과한 구간**에서
  > 발생한다. 무릎 부근에서는 LB 의 큐잉이 초과분을 흡수하므로 배정 정책의
  > 차이가 드러나지 않고, 흡수 여력이 소진되는 지점부터 격차가 열린다.

  실측이 이 서술과 정합한다: loc_pri 는 K비 0.90(L=200)에서 0.226 %로 멀쩡
  하고, K비 2.02(L=450)에서 4.16 %로 열화한다 — 무릎 통과 직후가 아니라
  **1.5 배 부근에서** 갈린다.
- **부기(§4.2 에 없던 두 번째 축)**: bl_lr 대비 격차는 **부하와 무관하게
  전 구간 존재**한다(L=200, K비 0.42 에서도 **8.2 %p**). 기전은 용량이
  아니라 **거리 × 밴드**다 — `s5_lr_L200_1` 에서 S3_search 위반 **100 %**
  인데 f_c p95 는 3.8 ms(서버는 한가하다). d_net(S3) 25 ms + 밴드 d_acc
  (4474 B / 1600 kbit ≈ 22 ms) + 처리시간 > SLO 45 ms 라 **구조적으로 불가**
  하다. SORTS 는 이 구간에서 search 를 전량 S1 로 보내(66.6 rps, K비 0.63)
  회피한다. 즉 "우위는 용량 경계에서 나온다"는 **최선 비교군(loc_pri) 기준
  서술**이고, **least-request 대비 우위는 용량 이전에 거리·클래스 축에서
  이미 발생**한다. 이 축은 §4.2 문구를 약화시키지 않고 범위를 넓힌다.

## 7. 검증·상태

- 23/23 DONE, SUSPECT 0, 조인율 전 런 1.0, 무인 중단 0.
- 동결 6파일 md5 **배치 전후 6/6 일치**(stage4-freeze 스냅샷 동일) — 이번
  단계는 **동결 해제 0**(부하·코호트·정책은 전부 config 키).
- DSB 컨테이너 **72/72, 기동시각 해시 배치 전후 동일**(2분 주기 대조,
  재시작 0). 원복: radio clear → cleanup_all → **precheck 통과**.
- 생성 건전성 보조: `.12` CPU 최대 3.1 %(프로브), S1 NIC 최대 9.9 Mb/s
  (링크 10 %) — 생성 한계는 커넥션 동시성(I-19)이고 이번 배치에서 갈래 (a)
  발생 0.
- 사전 스모크 1런(`stage5-smoke-20260812`)으로 매니페스트 키 검증 후 본
  배치 기동(I-18 (c) 규율 첫 적용).

## 8. 남기는 것

1. **1400 역전의 귀속 분해가 다음 실험**이다. 지금 데이터로는 (i) 용량
   테이블 비대칭(S2/S3 미측정), (ii) 고부하 진동(전환 245~264), (iii)
   총수요 > 총용량 자체 — 세 원인이 겹쳐 있다. S2/S3 의 `C_eff(band)` 를
   측정해 테이블을 대칭으로 만든 뒤 같은 점을 재측정하면 (i) 을 분리할 수
   있다(taskB3 §9 한계의 후속).
2. **격차 곡선의 K비 축**은 loc_pri·lr 이 서로 다른 축이라 단일 정규화가
   안 된다 — 7단계 그림은 **정책별 K비를 각자 축으로 병기**해야 한다
   (CSV 에 세 열 모두 방출).
3. P-S5-1 의 "역U" 표현은 이 데이터에 부정확하다 — **부호 반전을 포함한
   서술**로 바꿀 것을 제안한다(§0·§5).
4. bl_lr 의 전 구간 8~10 % 위반(거리 × 밴드 축)은 §4.2 에 없던 축이다.
   논문에서 이 축을 별도 문장으로 세울지는 G2 판단 사항.
