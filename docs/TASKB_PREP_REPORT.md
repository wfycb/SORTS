# 작업 B 착수 전 배치 보고 — 2026-08-10 (13:41~16:10)

배치 2개(A 9런, B 6+5런) 총 20런 **전부 DONE, SKIPPED/SUSPECT/FAILED 0**,
클린 복원 검증 완료(16:05 — 잔재 0, ogstun 깨끗, Envoy LIVE, sorts.yaml
기본 렌더 복귀·md5 일치, 컨테이너 72개 StartedAt 전부 8/2 = 재시작 0).
진행 로그 `runs/taskB-prep-20260810/PROGRESS.md`, 분석 `analysis/taskB-prep/`.
문서 개정은 TASKA_REPORT **개정 5** + ISSUES **I-12 확정 / I-13 신규** +
`status_v3_patch.md`.

## 1. 한 줄 결론 셋

1. **과제 A**: 가중 등가 부하는 **w=(search 1, reserve 0.278, recommend
   0.178), C(S1)=279** 로 좁게 식별되지만(CI ±7~20 %, 브래킷 검산 일치),
   사전 등록 홀드아웃 기준이 실패해 판정은 **"부적합"** — 성격은 함수형
   오류가 아니라 **측정 조성 밖 외삽 불가**(보간은 1.4×로 통과).
2. **과제 B**: **보류.** strict 의 클린×1400 붕괴는 결정적이 아니라
   **준안정 락인(3/7, 락인 시 70.0~71.0 %)**이고 far_tier 는 어트랙터를
   제거한다(5/5 안정). 기전은 완전 규명(stale_ttl 매개 주기-2 사이클),
   발생률 유의성만 미달(Fisher p≈0.24) — 트리거 통제 실험을 제안한다.
3. **과제 C**: 개정 5 완료 — 세 발현 통합 표(§4), 승격 확정 기술(§5.5,
   철회 이력·차이 명시), 서사 변경(§5.6 "변별은 용량 경계에서 나온다"),
   개정 4 잔재 모순 문단 정리, I-12 확정·I-13 등록. v3 문서는 이 머신에
   없어 병합 패치(`status_v3_patch.md`)로 대체.

## 2. 과제 A — w/C 추정 (상세 `weight_estimation.md`)

| 파라미터 | 점추정 | 90 % CI (부트스트랩 100, 조성 층화) |
|---|---|---|
| w[reserve] | **0.278** | [0.265, 0.315] |
| w[recommend] | **0.178** | [0.165, 0.215] |
| C(S1) | **279** search-등가 rps | [273, 287] |

- 데이터: 신규 M1/M2/M3 9런 + 야간 M0 8·search 단독 8 재사용 (전부 16conn
  동일 절차). 정성 확인 — M2(reserve 중심) 850 붕괴 vs M3(recommend 중심)
  850 정상: reserve > recommend 비용.
- 적합 경위 정직 기록: 1차 적합은 u-캡 퇴화(모순 해 통과) → **모형 의미론
  제약**(비붕괴 L<0.97C, 붕괴 L≥0.97C — 붕괴 4런은 SSE 제외·제약 전용)
  추가로 안정화. 튜닝 아님, 수정 전후 코드 보존.
- 홀드아웃(사전 등록 = M3): train 0.495 vs holdout **4.90 (9.9×>2×) 실패**.
  보조 진단 — M1 홀드아웃 0.72(1.4×) 통과, M2 12.3 실패: **어느 클래스든
  고비중 조성을 빼면 그 방향 상한 브래킷이 사라져 외삽이 폭주**한다.
  → 판정 "부적합" 기록, 실사용은 **측정 조성 볼록포 안 조건부**.
- S2/S3 등가 용량(파생, w 사이트 공유 **미검증** 가정): C(S2)≈520,
  C(S3)>832. S2 조성 스윕은 여유 시간을 과제 B 증설에 배정해 생략(§5).

## 3. 과제 B — D1 반복 (상세 `d1_repeat.md`)

- far_tier 0.137~0.166 % (n=4, 전환 0) vs strict_far **쌍봉**: 락인 3/7
  (70.0/70.7/71.0 %, 유닛당 420전환/426틱 주기-2, 6유닛 동조) / 안정 4/7
  (0.17~0.18 %, 그중 2런은 t+6 s 트리거 후 회복). 야간 포함 strict 4/8,
  far 0/5.
- **3성분 분리 성공**: 진동(사이클 구조 자체) / 실과부하(service 위반
  20~51 %, S2 체류 틱 slack −89~−103, strict_6 은 초당 S2 유입>1000 이
  47 % 초) / HOL(corrected−service 갭 +22~+50 %p, 소형 클래스일수록 큼).
  등록했던 "초당 S2 유입>1000 초 비율" 지표는 1 s 교대와 초 버킷 위상
  때문에 락인 런에서도 0 이 나올 수 있음 — 지표 명세 실패로 기록
  (slack_s2 관측 음수가 올바른 증거).
- **유지 기전 규명**: 이탈 사이트의 관측이 stale_ttl(2 s)로 만료 → prior
  복귀 → 재진입. S3 물리 용량 문제 아님(t1c: site_s3 고정 1400~1600 =
  위반 0.1~1 %).
- **판정: 보류** (기준 1 — 쌍봉이 반범위 폭파, 기준 2 — 3/7 만 발현).
  올바른 주장 형태는 "항상 붕괴"가 아니라 **"어트랙터 노출 vs 제거"**.
  후속 제안: **트리거 통제 실험**(초기 요동을 결정적으로 주입 → 락인
  여부 비교, n=5/arm 이면 충분 추정) 또는 strict n≥15 발생률 검정.

## 4. 작업 B 설계에 넘길 입력

1. **용량 모형 형태**: `L_eff(site)=Σw·rps ≤ 0.9·C(site)` — 지시대로 밴드는
   d_acc 로만(이중 계산 금지). w/C 는 §2 값, **유효 범위 = 측정 조성
   볼록포**. 여유율 0.9 는 무릎 날카로움(±20 rps 에서 2~5배) 때문에 필수.
   운영 조성이 바뀌면 그 방향 브래킷 2점 선측정이 전제 조건.
2. **검증 조건 — 두 개 병행을 권고 (D1 단독 불충분)**:
   - ① **양 코호트 극단(1600 k×800)** — radio 축 대표, before 확립
     (far 70.54±0.72 / strict 95.03±0.98, n=4). 기대: 용량 인지 배정 시
     search 초과분(266−~215)만 이월 → **총위반 ≈6~9 %** (야간 §4 추정,
     w-모형과 자기일관 — 266 rps ≈ 0.95C(S1)).
   - ② **클린×1400 트리거 통제** — 진동 어트랙터 차단 검증. D1 원판은
     락인이 확률적이라 A/B 표본 효율이 나쁨 → 트리거 주입판으로 전환.
     기대: 용량 항이 있으면 S2 이동 자체가 차단(1400>C(S2)≈520 등가)되어
     락인 불가능 — before = 락인 시 70 %, after 기대 ≈0.2 %.
3. **세 발현 통합(§4 개정 5)**이 작업 B 의 동기 서사: 결정이 자기 부하
   효과를 모른다 → I-9(진폭)·I-10(엣지 침식)·I-13(락인). far_tier 는
   각각 완화·이동·제거로 작용했으나 용량 항 없이는 불완전.

## 5. 판단해서 결정한 것

1. **B 증설을 strict 가중(+4 strict, +1 far)으로** — 쌍봉 발견 후 정보가
   부족한 쪽은 strict 락인 확률이었다. far 는 이미 4/4(야간 5/5) 안정.
2. **S2 조성 스윕(§2.4 "여유가 있으면") 생략** — §5 규칙("여유는 과제 B
   n 증설에")이 우선한다고 판단. C(S2)=520 은 미검증 파생값으로 명시.
3. **적합기 제약 추가**(모형 의미론 강제) — 1차 퇴화의 원인이 구현 누락
   이었으므로 수정하고 경위를 문서에 남김. 홀드아웃 실패 판정은 그대로
   기록(기준 완화 없음).
4. **개정 번호 5** — 지시서는 "개정 4"라 불렀으나 TASKA_REPORT 에는 C1
   세션이 쓴 개정 4가 이미 있다. 문서 내부 이력과의 일관성을 택했다.
5. **v3 문서 부재 처리** — 전역 검색으로 부재 확인 후 병합 패치 파일로
   대체(추측 편집 금지 원칙).
6. B 런은 disturb=none 이라 probe_buckets 가 정의상 비적용(밴드 버킷
   자체가 없음) — "전 런 적용" 지시의 취지(충돌 검출)는 무선 셰이핑이
   있을 때만 유효하므로 위반 아님으로 판단, 기록.

## 6. 실패·SKIP·SUSPECT

**없음** (20/20 DONE). 조인율 전 런 1.0. 락인/비락인은 실패가 아니라 결과.

## 7. 검증하지 못한 채 남긴 것

- **w 의 사이트 간 공유** (C(S2)≈520, C(S3)>832 는 가정 파생값).
- **락인 발생률의 유의성** (3/7 vs 0/4, p≈0.24) — 트리거 통제로 우회 권고.
- 락인 트리거(t+6 s 요동)의 정체 — 워밍업 종료 직후 과도인지, 관측 창
  경계 효과인지 미규명 (락인 여부와 무관하게 전 런에서 발생).
- w 추정의 within-run 비정상성 — reserve 는 런 중 문서 누적으로 비용이
  자란다(런당 최대 114 k 삽입). 런 단위 리셋으로 통제했지만 잔여 영향
  미정량.
- HOL 성분의 생성기 모형 의존(기존 단서 유지).

## 8. 다음에 결정할 것

1. **트리거 통제 실험 승인 여부** — D1 축 승격의 최단 경로 (런 ~12개,
   ~2시간). 주입 방식(부하 스파이크 vs 관측 오염)은 설계 시 제안.
2. **작업 B 검증 조건 2개 병행안 승인** (§4-2).
3. **C(S2)/C(S3) 직접 측정 여부** — S2 조성 2점(~15분)이면 w 공유 가설
   1차 검증 가능. 작업 B 배치에 끼워 넣을지.
4. **w 모형의 논문 위상** — "부적합(외삽)" 판정을 안고 조건부 사용으로
   갈지, 조성 격자를 더 떠서 전면 채택할지.
