# 작업 B 후속 보고 — soft assignment · 검증 ① 재실행 (2026-08-10 20:52~22:45)

배치 2개(s0b 6런, v1s 7런) 총 13런 전부 DONE, **SUSPECT/FAILED/SKIPPED 0**.
클린 복원 검증 완료 (기본 렌더 재배포 md5 일치, 잔재 0, 컨테이너 72 재시작 0,
런타임 가중치 비정상 키 0, 디스크 172 GB). 진행 로그
`runs/taskB2-20260810/PROGRESS.md`, 분석 `analysis/taskB2/`. 사전 등록
(목적함수·배정 규칙·판정 기준·해석 예측·드리프트 예측)은 전부 실행 전
PROGRESS.md 에 기록돼 있다.

## 1. 한 줄 결론 (단계별)

- **가드 (§5)**: 파일 잠금 가드 구현·활성 — 배치 시작 시 6파일 md5 스냅샷,
  런마다 대조, 변경 시 그 런 SKIPPED. 두 배치에서 동작 확인 (위반 0).
- **단계 0a — C(S3)**: **w 공유 불성립 (S3)** — 조성별 브래킷 교집합 공집합
  ([852.8, 840.5] eq). 조성 조건부 **C(S3|M0) = 865 eq** (CI [795, 865],
  rmse 0.069) 채택, 가정 명시.
- **단계 0b — 드리프트**: **단조 아님** 확정. 담체 = 스케줄 밀림(HOL),
  원천 = 포화 S1 f_c p95 +3 ms. 시간대(코테넌트) 가설은 §G 예측으로 걸었고
  **오늘 밤 결과가 기각** — 원인 미규명으로 남김 (후보 좁힘, §3).
- **단계 1 — soft assignment**: 구현 완료. selftest(기존+soft 6케이스) 통과,
  **회귀 96행 동일 (soft off = 작업 B 결정 단위 동일)** — §3.5 정지점 통과.
- **단계 2 — 검증 ①**: **성공 — 사전 등록 기준 3종 전부 통과.**
  soft on **28.06 ± 3.42 %** vs off **74.60 ± 1.42 %** (−46.5 %p).
  6~9 % 미달 — 원인 실측 규명 (§5): 관측 slack 진동이 S1 활용을 반토막.

## 2. C(S3) — w 공유 판정과 채택값 (상세 `s3_wshare.json`, `s3_c_m0.json`)

- 신규 6점 (48 conn/코호트 — 야간 t1c S3 와 동일 프로파일, 그 4점 재사용):
  M0 2100(붕괴·달성 1774)/2500(붕괴·2093), M1 900(비붕괴)/1450(붕괴·1255),
  M2 2400(**완주·비붕괴, viol ≤1.4 %**)/3300(붕괴·2764).
- 브래킷 (S1 w 고정, eq): M0 [759, 840.5] · M1 [726, 1013] · M2 [853, 982]
  → 교집합 **공집합** (M2 비붕괴 853 > M0 붕괴 840.5). C-only 적합 불가,
  자유 w 적합(west 격자)도 해 없음 → **사전 등록 기준으로 공유 불성립**.
- 해석: reserve-중 조성(M2)이 S3 에서 선형 w 예측보다 싸다 — 야간
  capacity_knee.md 의 조성 의존 경고가 S3 에서 실측 확인. S2 는 공유가
  성립했으므로 (단계 0, 잔차 0.218) **w 모형 유효 범위는 조성 볼록포에
  더해 사이트에도 의존**한다.
- **채택**: 검증 조건이 M0 조성이므로 조건부 **C(S3|M0)=865** (M0 계열 6점
  적합, CI [795, 865]). 전 조성 단일 C(S3) 는 부재 — **조성 변경 시 그 방향
  브래킷 재측정 필수** (tmpl `c_eq.S3` 주석에 동일 명시). M1/M2 조건부 C 는
  브래킷만 확보, 적합은 안 함 (현 실험 미사용).
- 기록: M0 붕괴 달성치 불일치 (2100→1774 vs 2500→2093) — 붕괴 하 달성률은
  하중 깊이 의존. 브래킷은 min 사용.

## 3. 드리프트 진단 — 판정: 단조 아님 / 원인 미규명 (`drift_diag.json`)

- **단조 기각**: 07:10/07:19 런이 04시 값과 일치 (2 h 공백에도 증가 없음),
  배치 내 기울기 부호 혼재 (v1 far+on −5.2 %p/hr vs 야간 −0.35).
  reservation 문서 수 이력은 러너가 reset 출력을 버려 부재 — 단 매 런 drop
  설계라 누적 가설 자체가 약함. 온도/주파수 과거 계측 없음 — **이번 배치부터
  런마다 5호스트 thermal.json 수집** (x86_pkg_temp 62~65 °C 관측, acpitz 는
  고정값으로 판독 — 기록만).
- **분해**: +4.4 %p 는 전 사이트×클래스 셀에 분산인데 **svc_p95 는 야간·저녁
  완전 동일** (S2 rec 18.59↔18.60, S3 rec 28.23↔28.23 등) — 증가분은
  corrected−service = 스케줄 밀림 (커넥션 HOL). 움직인 유일한 서버 지표는
  포화 S1 search f_c p95 (+3 ms). 포화점에서 미세 용량 변화가 대기시간을
  크게 흔드는 구조.
- **§G 사전 예측과 기각**: "시간대(코테넌트) 요인이면 밤에 70.5 로 회귀"를
  실행 전 등록 → 실측 밤 22시 soft off = **74.60** (오후 74.21 유지, 회귀
  없음), strict 참조는 98.10 (오후 95.03 대비 +3.1). **가설 불지지 — 기각.**
- **판정: 미규명** (§F 규정대로 추측으로 채우지 않음). 남은 후보: (a) 야간
  t2 와 오늘 사이의 코드 개정 (작업 B 의 obs/sorts_ctl 확장 — off 결정은
  회귀로 동일하나 obs 부하·tick 타이밍 등 2차 효과 미배제), (b) S1 호스트
  상태의 반영구 변화. **판정 실험 = 야간 시간대(03~07시)에 현 코드로 동일
  arm 재실행** — 값이 70.5 로 돌아오면 (b)/시간대, 74.5 유지면 (a)/코드.

## 4. 등록한 목적함수와 배정 규칙 (사전 등록 PROGRESS.md §A~B)

- **목적함수 (B) 위반 건수 최소화**. 근거: 주 지표가 위반율, 야간 6~9 %
  추정이 (B) 계열. (A) 초과량 합은 사후 분석으로만 (§5 말미).
- **배정 규칙**: EXPECTANT + capacity on + soft on 유닛 한정. 후보 = 전
  사이트. room = HEADROOM·C − planned (등가, §3.3-2 지시 그대로). badness =
  max(0, −slack) 오름차순 그리디 (동률 = 원거리 우선 — far-first 유지, 소형
  클래스가 원거리 양슬랙으로 가서 S1 room 을 search 에 남기는 암묵 클래스
  우선순위). 전 room 소진 후 잔여는 S1 (기존 폴백과 동일 사이트 = "아무것도
  안 함"과의 차이를 room 내 배정만으로 한정). 표현 = **기존 weighted_clusters
  런타임 가중치 정수 %** (새 메커니즘 없음; 라이브 70/30 설정·판독 확인).
- `feasible` 비공집합 경로 **불변** — 회귀 96행 동일. `soft_assign: off`
  스위치, capacity off 시 무효 (selftest 케이스).
- 기록: decisions 7열 추가 (soft_applied, carry_s1~s3, soft_overflow_eq,
  soft_objective_eq, soft_weights). blocked_by 기존 유지.

## 5. 검증 ① 결과 — 사전 등록 기준 3종 판정 (`v1s_results.json`)

조건: 1600 k × 800 양 코호트 극단, 교대 배치 (on,off,on,off,strict,on,off).

| arm | both 창 위반율 | 평균±반범위 |
|---|---|---|
| far+cap on+**soft on** (n=3) | 30.110 / 30.457 / 23.621 | **28.06 ± 3.42** |
| far+cap on+soft off (n=3) | 74.452 / 76.089 / 73.250 | 74.60 ± 1.42 (작업 B 74.21 재현) |
| strict+off (n=1, 참조) | 98.095 | – |

1. **유의 개선 — 통과**: 차 46.5 %p > 임계 4.8 %p (반범위 합). 로그 스케일
   2.7배로도, (A) 목적함수로도 (아래) 같은 방향.
2. **발동 — 통과**: soft 79~81틱 (search 유닛만 — 사전 예측 D-2 적중).
   off 런의 blocked_by=slack 108~110틱이던 창 정확히 그 자리 (on 런 잔여
   slack 54 + both 25~27; slack 표기가 남는 틱은 soft 배정이 붙은 EXPECTANT).
3. **이월 목적지 일치 — 통과 (런1 편차 명기)**: 도착(send_ts) 기반 —
   의도 몫 S1/S2/S3 ≈ 30/69/0.5 % vs 실측 on_2 36/63/0.4, on_3 37/62/0.4
   (**≤6 %p**), on_1 은 56/44/0.5 (25 %p 편차 — 1 s 진동 위상·적용 정렬
   잡음, 틱 단위 MAE 11.9 %p·p90 19.4 %p). S3 ≈ 0 은 전 런 일치.

**세 기준 충족 — 성공 판정** (§4.1: 6~9 % 미달이어도 유의 개선이면 성공).

**6~9 % 미달의 원인 (실측)**: 예측 D-1 적중. S1 평균 적재 128 eq ≪ room 251
— S1 채움 틱에 f_c 폭등 (S1 search fc p95 45.3; room 0.9C=251 이 밴드 하
SLO-유효 용량 ~210-220 보다 큼) → 관측 slack 음수화 → 다음 틱 S1-회피
(S2:100) → 회복 → 재채움의 1 s 진동, 회피 duty ~2/3. 그 결과 이월 search 가
야간 추정(51 rps)의 ~2.7배(138 rps)가 되고, S2 이월 search 는 slack −3.9 의
구조적 경계라 69 % 위반. 잔여 28 % 구성: S2 search 69 %·S1 search 51 %·
recommend 11~32 %·reserve 3~5 %.

**(A) 목적함수 사후 평가**: Σ max(0, corrected−SLO) = on 932~2521 vs off
14,720~18,018 ms/s — 6~19배 개선, (B) 판정과 동일 방향 (등록 판정은 B).

## 6. soft_applied / carry_over 분포

- 발동은 **정확히 필요한 곳에만**: both 창 search 유닛 79~81틱, 그 외
  (c1only 창·소형 클래스·pre/post) 0. reserve/recommend 는 전 구간 정상
  far_tier 경로 (예측 D-2).
- carry (soft 틱 합, 창 평균): S1 ≈ 57~61, S2 ≈ 131~133, S3 ≈ 0.6~0.9 eq rps,
  **overflow 0** (총 room 1463 eq ≫ 수요 — 엣지 포화 창은 용량 부족이 아니라
  배정 부재였다는 것의 실측 확인).
- 가중치 히스토그램: S2:100 (S1-회피) ≈ 58/86 vs S1:9x|S2:x (S1-채움) ≈ 28/86
  — §5 의 진동 구조 그대로.

## 7. 내가 판단해서 결정한 것과 근거

1. **C(S3) 를 조성 조건부로 채택** — w 공유 불성립 상황에서 전 조성 평균은
   어느 조성에도 안 맞는다. 실험 조성(M0)에 유효한 값 + 한계 명시를 택함.
2. **badness 를 라이브 slack (관측 포함) 으로 계산** — 관측 계층의 의미
   유지, 과부하 오염은 room 캡이 흡수. 진동 위험은 예측 D-1 로 등록하고
   실측으로 확인했다 (적중 — §5).
3. **overflow 는 S1** — 기존 EXPECTANT 폴백과 같은 사이트로, soft = "room 내
   배정만 추가"라는 최소 차이 원칙. 실측 overflow 0 이라 미발동.
4. **기준③을 도착(send_ts) 기준으로 판정** — 완료 시각 기준 집계는 HOL
   귀속 지연으로 왜곡됨을 확인하고 도착 기준으로 평가 (완료 기준 수치도
   analysis 에 남김).
5. 드리프트는 §G 예측 기각 후 **미규명으로 남김** — 규정(§F)대로.

## 8. 실패·SKIP·SUSPECT

**13/13 DONE, SUSPECT/FAILED/SKIPPED 0.** 가드 위반 0 (작업 B 의 "배치 중
tmpl 편집" 실수는 이번에 절차로도 회피 — 배치 사이에만 편집).

## 9. 검증하지 못한 채 남긴 것

- **6~9 % 도달은 미달성** — 진동 억제 (SLO-aware room 또는 채움 히스테리시스)
  는 §10-1 의 별도 설계 결정. 현 결과는 "배정 부재 대비 46.5 %p 개선"까지.
- S3 조성별 용량 곡면 (M1/M2 조건부 C 미적합). w 모형의 사이트 의존 기전
  (왜 S3 만 불성립인가 — d_net 예산 잠식 가설은 미검증).
- 드리프트 원인 (§3 판정 실험 미실행 — 야간 재실행 필요).
- soft on 이 검증 ② 조건(클린×1400 트리거)에서 락인·진동에 주는 영향
  (soft 는 EXPECTANT 한정이라 이론상 무영향이나 미실측).
- 기준③ 런1 의 25 %p 편차의 정확한 기전 (진동 위상 잡음으로 추정, 1런뿐).
- thermal.json 계측은 시작만 — 드리프트와의 상관은 데이터 축적 후.

## 10. 다음에 내가 결정해야 할 것

1. **진동 억제 설계 승인 여부** — 28 % → 한 자릿수의 유일한 경로로 보인다.
   후보: (a) search 류 클래스의 S1 채움 상한을 SLO-유효 용량으로 (밴드 결합
   용량, capacity_knee.md 실측 210-220 — 단 "클래스별 용량은 조성 의존"
   경고와 충돌하지 않게 설계 필요), (b) 채움 duty 히스테리시스 (2틱 유지 등).
   둘 다 결정식 확장이라 별도 사전 등록 필요.
2. **드리프트 판정 실험** (야간 03~07시 동일 코드 재실행) 을 언제 돌릴지 —
   작업 I(하룻밤 반복)의 해석 타당성이 걸려 있어 그 전에 권고.
3. **S3 w 공유 불성립의 논문 처리** — "선형 w 모형 유효 범위 = 측정 조성
   볼록포 × 사이트" 로 확장 서술 + M1/M2 조건부 C 추가 측정 여부.
4. 검증 ① 성공의 논문 위상 — I-14(손실 배분)를 용량 제약과 분리된 절로
   쓸지, n 확대 필요 여부 (현 n=3, 반범위 3.4 %p vs 효과 46.5 %p).
