# 1단계 보고 — Envoy 비교군 정상화 + 제어 주기 파라미터화 (2026-08-11 14:52:29)

자동 생성 (`analysis/stage1/s1_report.py`, 드라이버 무인 경로). 원자료 `runs/stage1-20260811/`, 분석 산출 `s1_results.json`, 배치 상태 `driver_status.json`.

## 1. 한 줄 결론 (작업별)

1. **작업 1-1 outlier 비교군**: `bl_od` 추가·기동 확인, 서버 축 4 런 수집 — 수치는 §5.1.
2. **작업 1-2 locality+priority**: `bl_loc_pri` 추가. 전원 healthy 에서 P0(S1) 100 % 라는 문서 기대 거동을 스모크로 실측 확인 (12/12 S1). 엣지 축 결과는 §5.2.
3. **작업 1-3 active HC**: 전 클러스터 HC 변형 배포·검증. **관측 오염 없음**(HC 단독 65 s 구간 access log 증가 0 B). 노드 장애 before/after 는 §4.
4. **작업 1-4 주기 파라미터화**: `ctl_period_s` 도입(기본 1.0, 주기 **변경 없음**). 회귀 통과 (앞 12열 96행 동일).

## 2. 동결 해제 기록 — 변경/미변경 파일

| 파일 | 상태 | b3-freeze md5 | 현재 md5 |
|---|---|---|---|
| sorts_ctl.py | **변경** | `8ff7b206` | `97d63b83` |
| obs.py | 미변경 | `b9fac68d` | `b9fac68d` |
| sorts.yaml.tmpl | **변경** | `e7be4f2b` | `006047f8` |
| run_all.py | **변경** | `824fcaad` | `86db0c75` |
| gen_envoy_v10.py | **변경** | `cc18cf55` | `e10a70f8` |
| envoy_keys.json | **변경** | `a082dafa` | `b5b47c99` |

- 해제 범위 준수: `obs.py` **미변경**(md5 동일), 결정 로직(`decide`/`decide_live`/용량/손실 배분) 무수정 — `sorts_ctl.py` 변경은 주기 읽기 2줄뿐이며 회귀로 결정 동일성을 확인했다.
- `envoy.yaml`(.43 /etc/envoy): 렌더 산출물. 변형 2종(hc_off/hc_on) — 백업 `envoy.yaml.20260811-pre-stage1.bak`.
- 해제 기록·재동결 예정(2단계 종료 후)은 `docs/FREEZE.md` 해제 기록 1.
- 작업 전 백업: `.40:~/exp/*.20260811-stage1.bak` 6종.
- 미변경 유지: 관측 파라미터(WINDOW_S 2.0, n_min 100/20, stale_ttl 2.0, FILL_RATIO 0.8) 튜닝 없음.

## 3. 비교군 설정 블록 — 기본값 이탈 항목과 이유

설정 블록 **전문**은 `docs/baselines.md` (재현성). 요약:

| 비교군 | 기본값에서 벗어난 항목 | 비고 |
|---|---|---|
| `bl_od` | **없음** (`outlier_detection: {}` = 문서 기본값 전부) | consecutive_5xx 5 / interval 10 s / base_ejection 30 s / max_ejection 10 % / enforcing 100 % |
| `bl_loc_pri` | **없음** (overprovisioning factor 1.4 미설정) | 우선순위 S1→S2→S3, panic threshold 기본 50 % |
| active HC | **없음**, 단 4 필수 필드는 Envoy 에 기본값이 없어 k8s probe 기본값 채택 | interval 10 s / timeout 1 s / unhealthy 3 / healthy 1, path `/`(DSB 정적, DB 무접촉) |

판단 근거는 `docs/baselines.md` 에 항목별로 기록했다 — 요지는 (a) 단일 출처에서 세트를 통째로 가져오고 항목별 짜깁기를 하지 않음, (b) 조정 가능한 민감도(overprovisioning)는 어느 방향이든 튜닝 시비가 성립하므로 문서 기본값이 유일한 방어 위치.

## 4. 노드 장애 before/after + 복구 3지표

| 런 | arm | 차단창 위반% | 재개 지연 s | 정상화 지연 s | 드레인 손실 (위반/총) |
|---|---|---|---|---|---|
| s1nf_repro_1 | SORTS (HC 없음) | 100.0 | 0.0 | 93.6 | 86558/110803 (78.1%) |
| s1nf_hc_1 | SORTS + HC | 100.0 | 0.0 | 74.7 | 94341/97180 (97.1%) |
| s1nf_hc_2 | SORTS + HC | 100.0 | 0.0 | 69.6 | 87345/90901 (96.1%) |
| s1nf_hc_3 | SORTS + HC | 100.0 | 0.0 | 68.8 | 87325/90476 (96.5%) |
| s1nf_lr_1 | bl_lr + HC | 33.4 | 9.8 | 10.8 | 34/8795 (0.4%) |
| s1nf_lr_2 | bl_lr + HC | 32.6 | 7.7 | 8.7 | 43/7202 (0.6%) |
| s1nf_lr_3 | bl_lr + HC | 37.5 | 4.5 | 5.5 | 40/4801 (0.8%) |

지표 정의 (사전 고정, 사후 조정 없음): 재개 = 해제 후 S3 완료 트래픽 첫 관측; 정상화 = 10 s 롤링 위반율이 pre 수준(pre 평균 + 1σ) 이하로 떨어져 10 s 유지되는 시각; 드레인 손실 = 해제~정상화 구간의 위반 건수. 정상화 미달성이면 드레인 구간은 측정 종료까지로 잡았다 (하한).

### 4.1 복구 3지표 최종 정의 (정정·부기, STAGE2 §0.2-1)

- **재개 지연** = 해제(unblock) → `s1_share_ts.csv` 1 s 버킷에서 S3 완료 트래픽(`n_S3`>0)이 처음 관측되는 버킷. 해제 시각을 포함하는 버킷부터 보며 음수는 0 으로 절단 (분해능 1 s).
- **정상화 지연** = 해제 → 10 s 롤링 위반율이 pre 수준(pre 평균 + 1×pre 표준편차, 1 s 버킷 기준) 이하가 되고 이후 10 s 유지되는 첫 시각. 판정은 롤링 창(폭 10 s)의 시작점 기준.
- **드레인 손실** = 해제 버킷부터 정상화 시각 직전까지 1 s 버킷 구간의 **위반 건수** (분자·분모 모두 기록). §4 표의 89,670.3 은 SORTS_HC arm **런별 값** 94,341 / 87,345 / 87,325 의 **n=3 평균**이다 — 합계가 아니다 (per-run 값은 §4 표의 "드레인 손실" 열).
- **위반의 카운트 대상** (분자): ① 비-200 응답(503 UF·504 등 — 러너가 수신한 오류 회신), ② 러너 타임아웃(`status=err:TimeoutError`, REQUEST_TIMEOUT 5 s — 미회신도 생성기가 행으로 기록하므로 카운트에 **포함**된다), ③ 200 이지만 기대 바이트 이탈(무효 바이트: search 4474±10 %, reserve 36·recommend 200 정확 일치), ④ 200·바이트 정상이지만 `corrected_ms` > SLO. 분모 = 같은 구간의 전체 기록 행(warmup=0).
- **원자료 재계산 경로**: `runs/stage1-20260811/{nf-repro,nf-hc}/<run>/load_c{1,2}.csv` 의 `end_ts`·`status`·`bytes_recv`·`corrected_ms`·`warmup` + 배치 디렉터리 `nf_events.json` 의 `block_ts`/`unblock_ts`(.40 시계) + `meta.json` 의 `clock.d12_s`(시계 보정). 구현 = `analysis/stage1/s1_analyze.py::recovery()`.
- **§5 미결("백로그 드레인 67~75 %") 종결 경위**: 야간배치2 는 드레인 구간의 **끝을 정의하지 못해** 비율(67~75 %)만 보고했다. 이번에 정상화 시각(사전 고정 기준)으로 창을 닫아 3지표로 확정했고, 미정상화 런은 측정 종료까지를 하한으로 명시하는 규칙을 함께 고정했다 (이번 배치는 전 런 정상화 달성).
- **의미론 주의**: 이 생성기는 절대 스케줄(개방 도착) + **커넥션당 직렬 전송**(포트 고정 16개)이다. DROP(silent) 동안 워커가 REQUEST_TIMEOUT(5 s)까지 매달리고 밀린 스케줄 슬롯이 해제 후 연속 발사되며, `corrected_ms`(=end−sched)가 적체 시간을 포함해 위반으로 계수된다 — 이 폐루프성 적체가 드레인 손실을 키우는 동역학이다. REJECT(RST) 였다면 즉시 오류 회신으로 다음 슬롯 진행이 빨라 백로그 동역학이 다르며, 실제 장애는 양쪽(무응답/즉시 거절)에 걸친다 — 본 수치는 DROP 쪽의 값이다. (새 arm 은 추가하지 않는다 — 정의·조건 명시로 충분, STAGE2 지시 §0.2-1.)

### arm 요약 (평균 ± 표본표준편차)

| arm | n | 차단창 위반% | 재개 s | 정상화 s | 드레인 위반건 |
|---|---|---|---|---|---|
| SORTS_noHC | 1 | 100.0 ± 0.0 | 0.0 ± 0.0 | 93.6 ± 0.0 | 86558.0 ± 0.0 |
| SORTS_HC | 3 | 100.0 ± 0.0 | 0.0 ± 0.0 | 71.0 ± 3.2 | 89670.3 ± 4044.9 |
| bl_lr_HC | 3 | 34.5 ± 2.6 | 7.3 ± 2.7 | 8.3 ± 2.7 | 39.0 ± 4.6 |

### 개선 귀속 분리 — SORTS 는 보는가, Envoy 가 막는가

| 런 | 차단창 S3 ∈ feasible_set | 실제 S3 도달 비중 | HC 첫 실패 감지 (차단 후 s) |
|---|---|---|---|
| s1nf_repro_1 | 1.000 | 0.7310 (43936/60104) | — |
| s1nf_hc_1 | 1.000 | 0.1504 (8821/58657) | 28.2 |
| s1nf_hc_2 | 1.000 | 0.1478 (8984/60792) | 26.0 |
| s1nf_hc_3 | 1.000 | 0.1315 (7974/60638) | 24.6 |
| s1nf_lr_1 | — | 0.1005 (9596/95511) | 28.0 |
| s1nf_lr_2 | — | 0.0932 (8894/95455) | 26.2 |
| s1nf_lr_3 | — | 0.0651 (6225/95683) | 23.8 |

판정 규칙: S3 ∈ feasible ≈ 1.0 인데 실제 도달 비중이 0 에 가까우면 **SORTS 는 여전히 노드 생사를 못 보고 Envoy HC 가 막는 것** — 계층 분리의 직접 증거. 둘 다 0 이면 SORTS 도 뺀 것이라 귀속이 갈린다.


## 5. 서버 축 · 엣지 축 비교군 결과 (불리해도 그대로)

### 5.1 서버 축 — bl_od(outlier, 기본값) vs bl_lr

| 런 | 정책 | pre 위반% | during 위반% | post 위반% | during 분배 |
|---|---|---|---|---|---|
| s1lr_server_hc_1 | bl_lr | 0.08 | 0.07 | 0.12 | {'S1': 0.490775, 'S2': 0.295033, 'S3': 0.214191} |
| s1od_server_1 | bl_od | 0.08 | 0.08 | 0.10 | {'S1': 0.493431, 'S2': 0.293297, 'S3': 0.213271} |
| s1od_server_2 | bl_od | 0.08 | 0.11 | 0.05 | {'S1': 0.490218, 'S2': 0.296078, 'S3': 0.213704} |
| s1od_server_3 | bl_od | 0.07 | 0.12 | 0.08 | {'S1': 0.491686, 'S2': 0.293405, 'S3': 0.214908} |

비교 기준선(기존 값, 같은 교란 조건): PHASE4 S5_rr_server / S6_lr_server / S1~S4 SORTS. 이번 bl_lr 런은 **HC-on 상태의 재현성 대조**이므로, phase4 bl_lr 과의 차이가 크면 그 차이부터 설명해야 한다 (config 변형 효과).

#### 5.1.1 배치 3 완결형 — 3-arm (정정·부기, STAGE2 §0.2-2)

서버 축(tb-stress S3 `--cpu 2 --cpu-load 80`, t+120~240 s, c1 밴드 6,000 kbit, 800 rps) during **전체 위반%** (Σ n·rate / Σ n, 코호트 가중 — `s1_analyze.py::server_run()` 과 동일 산식):

| arm | during 위반% | 출처 런 | 조건 부기 |
|---|---|---|---|
| SORTS (const) | 1.680 / 1.389 | phase4-20260807 `S1_const_server` / `S3_const_server2` | HC off, 2026-08-07 코드(개정 전, strict_far) |
| SORTS (both) | 0.940 / 0.811 | phase4-20260807 `S2_both_server` / `S4_both_server2` | HC off, 2026-08-07 코드(개정 전, strict_far) |
| bl_rr | 0.108 | phase4-20260807 `S5_rr_server` | HC off |
| bl_lr | 0.075 / **0.07** | phase4 `S6_lr_server` / stage1 `s1lr_server_hc_1` | HC off / **HC on** (재현성 대조 — 0.075→0.07, config 변형 무영향) |
| bl_od | 0.08 / 0.11 / 0.12 | stage1 `s1od_server_1..3` | HC on, ejection 카운터 전부 0 (§10) |

- PHASE4_REPORT §6 표의 4.12/3.77(const)·2.17/2.07(both)·0.21(RR)·0.16(LR)은 **같은 런의 c1 search during 위반%** — 지표(부분모집단)가 다를 뿐 동일 원자료다.
- **taskA 계열은 이 표에 섞지 않는다**: taskA T3 far_tier+both **0.116 %** / T6 bl_lr **0.158 %** (위반(전체), TASKA_REPORT §2)는 같은 교란이지만 **개정 후 코드(집합 결정·far_tier)와 taskA 시점 config** 로 잰 별도 계열이다. 동일 정책인 bl_lr 조차 계열 간 0.075 ↔ 0.158 로 움직인다 — 두 계열을 한 표에 놓으면 코드 개정 효과와 arm 효과가 교락된다. 현행(b3 동결) 코드의 서버 축 SORTS 값은 taskA 계열이 정본이고, 위 표의 SORTS 값은 phase4 계열 내 비교 전용이다.

### 5.2 엣지 축 — bl_loc_pri(priority, 기본값) vs SORTS

| 런 | 정책 | both 창 위반% | during S1 몫 | S1 유입 rps | 무릎비 (400 기준) | f_c(S1) p50/p95 |
|---|---|---|---|---|---|---|
| s1edge_sorts_hc_1 | sorts_reactive | 6.132 | 0.122 | 97.7 | 0.24 | search 2.474/4.807 |
| s1locpri_edge_1 | bl_loc_pri | 100.000 | 1.000 | 800.0 | 2.00 | recommend 22.465/42.538; reserve 31.365/62.469; search 75.05/132.505 |
| s1locpri_edge_2 | bl_loc_pri | 100.000 | 1.000 | 800.0 | 2.00 | recommend 22.413/42.429; reserve 31.176/62.664; search 75.202/133.716 |
| s1locpri_edge_3 | bl_loc_pri | 100.000 | 1.000 | 800.0 | 2.00 | recommend 22.747/42.835; reserve 31.611/62.977; search 74.747/132.903 |

bl_loc_pri (n=3): **100.00 ± 0.00 %** vs SORTS 검증① 6.5 ± 0.85 % (HC off, taskB3).

런 조건 부기 (STAGE2 §0.2-3): `s1locpri_edge_1..3` 은 taskB3 검증①(4단계 누적표 최종단, `v1c_*`)과 **동일 부하·동일 판정창**이다 — disturb `seq_extreme`, 360 s, 16 커넥션 × 25 rps × 양 코호트(총 800 rps), 판정창 `windows.both`(`t2_policy_repeat` 정의 재사용, `s1_analyze.py::edge_run()`). 누적표(95→74.6→28.1→6.5 %) 옆에 나란히 놓을 수 있는 전제가 성립한다.

**브리지 런** (SORTS + HC, n=1): 6.132 % vs 6.5±0.85 (HC off) → 범위 안 — **HC 가 엣지 축 결과를 바꾸지 않음이 확인**되어 두 config 의 값을 나란히 놓을 수 있다.

엣지 과부하 확인: bl_loc_pri 의 위반이 낮게 나오면 S1 몫·유입 rps·무릎비(400)·f_c(S1) 로 **엣지 용량 포화가 발현했는지**를 본다. 무릎비 < 1 이고 f_c(S1) 가 평시 수준이면 이 조건에서는 엣지 과부하가 나타나지 않은 것이고, 그것도 결과다 — 엣지 주장은 다른 조건(더 높은 도착률 또는 더 긴 극단 창)에서 보여야 한다.


## 6. apply 경로 판정 + flush 실효 지연

### 6.1 apply 경로 (§1-5) — **변경 시에만 호출**

`sorts_ctl.py` 의 `run()` 은 유닛마다 결정 상태 문자열(클러스터명 또는 `W:` 비중 문자열)을 이전 값과 비교해 `changed` 일 때만 `apply()`/`apply_weights()` 를 부른다 — `runtime_modify` 는 **매 tick 이 아니다**. 결정이 안정된 구간에서 apply 비용은 0.

판정: **이번에 고칠 필요 없음.** 25 ms 주기에서도 정상 상태 오버헤드는 0 이고, 비용은 전환이 잦을 때만 발생한다. 다만 2단계에서 주기를 내리면 전환 빈도 자체가 오를 수 있으므로, **틱당 apply 호출 수**를 계측(decisions.csv `changed` 합)해 duty 를 산출할 것 (지시대로 이번엔 수정하지 않았다).

### 6.2 flush 실효 지연 (§1-6) — 800 rps, `--file-flush-interval-msec 100`

```
n=88014 bad=0 poll_ms=5
raw: p50=73.9 p95=119.2 p99=127.0 max=135.4 [ms]
corr: p50=52.6 p95=97.7 p99=102.1 max=106.1 [ms]
```

- `raw` = 로그 줄의 `START_TIME` → 그 줄이 tail 로 읽힌 시각. `corr` = raw − 필드16(요청 처리시간) — 즉 **응답 완료 후 로그가 보이기까지의 순수 지연**.
- 같은 호스트(.43) 시계라 클럭 오프셋 없음. 폴링 5 ms 가 측정 분해능.
- 해석: corr 상한이 ~100 ms 에 붙고 p50 이 그 절반 — 문서의 "버퍼가 차거나 간격이 지나거나 먼저 오는 쪽" 중 **간격 100 ms 가 지배**하는 형태(균일 대기). 이 부하에서는 버퍼 충만이 먼저 오지 않는다.
- **설정은 바꾸지 않았다** (지시대로 2단계 입력 전용).

## 7. 회귀 · 순도 · 오염 검증 결과

| 검증 | 결과 |
|---|---|
| §5.2 회귀 (`ctl_period_s: 1.0` + 기존 arm = 동결 코드) | **통과 (앞 12열 96행 동일)** |
| 컨트롤러 단위 검증 (3정책 + 용량 + soft + C_eff) | 통과 |
| Envoy config 유효성 (`--mode validate`, hc_off/hc_on) | OK / OK |
| 신규 클러스터 순도 (필드10/11) | `bl_od` 3사이트 분산·순수, `bl_loc_pri` 12/12 S1·순수 |
| §6.1.3 HC 관측 오염 | **없음** — HC 단독 65 s 에 access log 증가 0 B |
| §6.1.4 키 일치 (`envoy_keys.json` ↔ `/clusters`) | precheck 통과 (클러스터 12, 런타임 키 84, healthy EP 27/27) |
| §6.1.5 재기동 절차 | 전환마다 백업→교체(md5 대조)→`/ready` LIVE→healthy 27/27→런타임 키 84→컨테이너 24×3 무재시작 확인 |

## 8. 내가 판단해서 결정한 것과 근거

1. **HC 4 필수 필드에 k8s probe 기본값 채택** — Envoy 에 기본값이 없어 선택이 불가피. 단일 규약 세트를 통째로 가져와 짜깁기를 배제했고, 감지 ~30 s 는 그 세트의 산술적 귀결이지 목표가 아니다.
2. **HC 경로 `/`** — DSB frontend 정적 인덱스(200/1507 B, DB 무접촉 실측). `/health`·`/healthz` 는 404 라 사용 불가.
3. **HC 를 전 클러스터에 동일 적용** — 특정 비교군만 켜면 그 자체가 차별 조건이 된다. 삽입 수 == 클러스터 수를 생성기가 검사한다.
4. **config 변형 2종 + `envoy_keys.json` 의 `active_hc` 기록** — 사후에 어떤 변형에서 난 값인지 산출물로 식별 가능하게.
5. **`ctl_period_s` 신설 + `t_ctrl_s` 하위 호환 폴백** — 구 렌더본이 섞여도 조용히 다른 주기로 돌지 않게.
6. **차단 절차는 야간과 동일 스크립트 재사용**, 오케스트레이터에 **사건 epoch 기록(events.json)만 추가** — 복구 3지표가 절대 시각을 요구하는데 야간판은 HH:MM:SS 로그뿐이었다.
7. **정상화 기준을 pre 평균 + 1σ 로 사전 고정** — 사후에 기준을 고르면 어떤 값이든 만들 수 있다.
8. **무인 드라이버(nohup) 구성** — 세션이 끊겨도 배치→전환→원복→분석→보고서까지 완주. 비밀번호는 코드에 두지 않고 사전 조치(`/etc/envoy/envoy.yaml` 소유권)로 무권한화.

## 9. 실패 · SKIP · SUSPECT

없음 — 전 런 DONE, SKIPPED/SUSPECT/FAILED 0.

### 종료 상태 (원복)
- hc_off 원복: 성공
- iptables 5000 잔재 룰: 1
- precheck: 통과

## 10. 검증하지 못한 채 남긴 것

- **부분 장애(느려짐만, 응답 200)** 에서의 HC 거동 — 이번 차단은 무응답(DROP) 시나리오다. HTTP 200 이 계속 나오면 HC 는 통과시키므로 감지되지 않을 것이라 예상되지만 미실측.
- **outlier detection 의 지연 기반 감지 부재** — `bl_od` 는 오류 기반이라 grey(느림+200)를 정의상 못 본다. 서버 축 결과가 이를 보여주는지는 §5.1 수치로만 판단했고, ejection 계수 스냅샷(`envoy_stats_after_*.txt`)을 근거로 병기했다.
- **3노드에서 max_ejection_percent 10 % 의 첫 축출 허용 여부** — 문서가 명시하지 않아 스탯으로만 관측했다 (산술 추론 금지).
- **주기 변경 효과** — 이번엔 파라미터화만 했다 (기본 1.0 고정). 2단계 단독 ablation.
- **flush 설정 변경 효과** — 실측만 했고 `--file-flush-interval-msec` 은 그대로 둔다.

## 11. 2단계(주기 ablation) 설계에 넘길 입력

1. **apply 는 변경 시에만** — 정상 상태 오버헤드 0. 주기를 내릴 때의 비용은 apply 호출 수가 아니라 **전환 빈도**에 붙는다. 2단계에서 `changed` 합/틱을 계측해 duty 를 산출할 것.
2. **flush 실효 지연이 관측 신선도의 하한** — corr p50 ~50 ms, 상한 ~100 ms (§6.2). 제어 주기를 25 ms 로 내려도 **관측은 그보다 늦게 도착한다** — 주기 단독 효과와 로그 지연이 교락되므로, (a) flush 간격을 함께 내린 조건, (b) 내리지 않은 조건을 분리해 재야 귀속이 된다. 이번 실측이 그 설계의 근거값이다.
3. **관측 윈도(2.0 s)는 주기와 독립** — 주기를 내려도 윈도는 그대로 둘 수 있다(obs.py 설계). 주기 ablation 에서 윈도를 같이 만지면 귀속이 깨진다.
4. **`ctl_period_s` 렌더 경로가 준비됨** — manifest 에 값만 넣으면 arm 이 된다. 회귀 경계는 1.0.
5. **노드 장애 계층 결론(§4)** — 이 층은 주기를 줄여도 SORTS 가 메우지 못하는 영역인지, §4 의 귀속 분리 결과가 2단계 범위 설정에 그대로 입력된다.

---

본 보고서는 데이터에서 도출되는 문장만 담았다. 해석·판정 중 사람의 결정이 필요한 자리는 그 자리에 명시했다.
