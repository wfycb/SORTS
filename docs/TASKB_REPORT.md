# 작업 B 보고 — 용량 제약 구현과 검증 (2026-08-10 16:18~19:15)

배치 4개(s0 6런, v1 6런, v2cal 2런, v2 7런) 총 21런 전부 DONE, **SUSPECT 0,
FAILED 0** (s0 에서 내 절차 실수로 SKIPPED 후 재실행 6건 — §8). 클린 복원
검증 완료(19:05 — 잔재 0, 컨테이너 72 재시작 0, sorts.yaml 기본 렌더
capacity off 재배포·md5 일치). 진행 로그 `runs/taskB-20260810/PROGRESS.md`,
분석 `analysis/taskB/`. 사전 등록(판정 기준·해석 예측 2건)은 전부 실행 전
PROGRESS.md 에 기록돼 있다.

## 1. 한 줄 결론 (단계별)

- **단계 0**: **w 공유 성립** (잔차 0.218 ≤ 기준 1.0, 브래킷 교집합
  [474, 536]), **C(S2) = 515** (CI [495, 530]) — 결정식은 사이트별 C 만
  다르고 w 공통.
- **단계 1**: 용량 제약 구현 완료 — 예측 기반(b) 소스, selftest 8케이스·
  **회귀 96행 통과**, capacity off = 작업 A 와 결정 단위 동일.
- **검증 ①(엣지 경합)**: **기대(6~9 %) 미달성 — 사전 예측대로.** far+on
  74.21±1.97 ≈ far+off 74.91. 원인 실측: 실패 모드가 "전 사이트 slack<0
  → EXPECTANT"라 용량 분기가 발동할 자리가 없다(blocked_by=slack 108/108틱).
- **검증 ②(자가 진동)**: **성공 — 사전 등록 기준 3종 전부 통과.** 트리거로
  락인을 결정적으로 만든 뒤(off 3/3 락인, 70.4/70.6/63.9 %), **capacity on
  은 락인 0/3 (0.415/0.423/0.456 %)** — S2 이동 차단 틱 실측(3~4틱),
  진동 지표 소멸(전환 2520→27~31).

## 2. S2 측정 — w 공유 판정, C(S2) (상세 `s0_wshare.json`)

- 신규 6점(M0 900/1200, M1 570/750, M2 1250/1650, 32conn) + 야간 t1c 5점.
  붕괴점: M0 1200, M1 750(달성 664), M2 1650 — 세 조성 모두 브래킷 확보.
- S1 w(0.278/0.178) 고정 적합: 잔차 상대 RMSE **0.218** (기준 2×0.50=1.0),
  브래킷 교집합 **[473.9, 535.6] 비공집합** → **공유 성립** (§2.3 분기 1 →
  진행). C(S2)=**515**, 부트스트랩 CI [495, 530]; 파생값 520 이 CI 안.
- 미비: S2 단독 자유 w 적합(보조 검정)은 적합기 C 격자가 S1 범위(≤340)라
  미산출 — 주 판정 성립으로 추가하지 않음. S3 의 C 는 하한 832 유지
  (지시대로 — 실험 조건이 상한에 닿지 않음, v2 스파이크 중 일시 초과는
  발생했고 설계대로 차단됨).

## 3. rps[class][site] 소스 — (b) 예측 기반 채택, 근사 오차 실측

- 채택 근거(지시 권고 그대로): (a) 관측 기반은 결정→관측→결정 루프를 또
  만든다(I-9 부류). (b)는 자기 결정의 효과를 사전 계산 — 되먹임 없음.
- 구현: 유닛 총 도착률은 obs 가 XFF×클래스로 관측(외생량 — 총 도착은
  라우팅과 무관; 폐루프 생성기의 backlog 조절만 2차 효과로 남음을 명시).
  배분은 tick 내 유닛 고정 순서 그리디 — 이미 결정된 유닛의 예측 부하를
  누적(planned)하고 set_share 로 집합 내 비중을 근사.
- **근사 오차 실측**: {S2,S3} 예측 0.58/0.42 vs 실측 **0.580~0.582** (v1
  전 런, 오차 <0.2 %p). l_eff 열과 실측 등가 부하도 v1_results.json 에 병기.
- 코호트↔IP 매핑은 XFF 오름차순(관례) — 라벨 뒤집혀도 검증 조건이 코호트
  대칭이라 L_eff 불변(코드 주석·§9 참조).

## 4. 검증 ① — 기대치 대비와 규명 (상세 `v1_results.json`)

| arm | both 창 위반율 | blocked_by |
|---|---|---|
| far_tier+on (n=3) | 76.42/73.74/72.48 (**74.21±1.97**) | slack 108틱 (search 유닛 전부), capacity 0 |
| far_tier+off (n=2) | 74.99/74.84 | – |
| strict+off (n=1) | 95.03 | – |

- **기대 6~9 % 는 달성되지 않았고, 이는 실행 전 사전 예측 1 그대로다**
  (PROGRESS.md 16:18 기록): 1600 kbit 에서 banded search 의 S2 slack =
  −3.9 ms < 0 → §3.1 의미론(용량은 집합에서 **빼기만** 한다)으로는 slack
  음수 사이트로 **이월이 불가능**하다. 실측 확인: search 유닛의 EXPECTANT
  는 전부 blocked_by=slack — 관측 f_c 로 S1 slack 까지 음수여서 용량 분기는
  발동 기회조차 없었다. "이월된 search 가 S2 에서 어떻게 되는가"의 답:
  **이월이 없었다.**
- 함의: 야간 §4 의 6~9 % 추정은 "S1 에 215만 주고 초과분을 S2 로" 라는
  **손실 최소화 배정**을 암묵 가정했는데, 그것은 실행가능성(슬랙+용량)
  의미론 밖이다. 엣지 포화 창을 고치려면 별도 설계(EXPECTANT 시 최소
  피해 사이트 선택 / soft assignment)가 필요 — §10 결정 사항.
- 드리프트 기록: far+off 오늘 74.9 vs 야간 70.5 (+4.4 %p, 일중) — 같은
  배치 내 on/off 비교만 사용했다.

## 5. 검증 ② — 사전 등록 기준 3종 판정 (상세 `v2_results.json`)

트리거: 부하 스파이크 c1 +400 rps × 4 s @ t+30 (시각·크기 고정). 캘리브
2런 **모두 락인**(70.6/63.9 %, 전환 2520/1980) — 자연 발생률 3/7 을
결정적으로 만듦. 주입 조정 불필요(조정 이력 없음).

| arm | 위반율 | 전환합 | 락인 |
|---|---|---|---|
| strict+off (before, 캘리브 2 + 본 1) | 70.61/63.93/70.41 % | 1980~2520 | **3/3** |
| **strict+on** | **0.415/0.423/0.456 %** | 27~31 | **0/3** |
| far_tier+on | 0.134/0.144/0.148 % | 10~12 | 0/3 |

1. **락인 0/3 — 통과.**
2. **capacity 가 S2 이동을 막은 tick 존재 — 통과**: strict+on 런당
   capacity/both 6~7틱, 그중 cap_blocked 에 S2 포함 3~4틱. 락인의 유지
   기전(S2 실과부하 국면)이 정확히 그 지점에서 끊겼다.
3. **진동 지표 소멸 — 통과**: 전환 27~31(비락인 수준), 주기-2 구조 부재.
   사전 예측 2(EXPECTANT 폴백發 S3↔S1 왕복)는 **발생하지 않았다** —
   S1 이탈은 스파이크 중 1~2틱뿐이고, 스파이크가 끝나면 S3 관측이 건강해
   재락인 연료가 없다.

**작업 B(용량 층) 성공 판정.** far+on 도 스파이크 중 S2 를 1~2틱
차단({S3} 축소) — 용량 항이 far_tier 에서도 작동함을 확인.

## 6. blocked_by 분포 — 용량 항이 언제 작동했는가

- v1 (엣지 포화): far+on 런당 slack 108 / capacity 0 / '-' 216 — 이 창의
  병목은 용량 항 관할 밖(전 사이트 슬랙 음수).
- v2 strict+on: '-' 2549~2550 / capacity 4~5 / both 1~2 (2556틱 중) —
  스파이크 창에 집중, S2(3~4틱)·S3(일시, 스파이크 중 853eq > 749) 차단.
- v2 far+on: capacity 1~2틱(S2). 평시(클린 1400)는 far 분산 자체가 용량
  아래라 개입 0 — **용량 항은 정확히 필요한 순간에만 개입했다.**

## 7. 산출물·구현 요약

- `sorts_ctl.py`: decide_live(용량 필터, 정책별), shares_of/capacity_filter,
  HEADROOM=0.9(근거 주석: 무릎 ±20 rps 2~5배 — capacity_knee.md), decisions
  9열 추가(unit_rate_rps, l_eff_s*, cap_ok_s*, blocked_by, cap_blocked_sites).
  밴드는 용량 축에 없음(d_acc 로만 — 이중 계산 금지 준수).
- `obs.py`: XFF×클래스 도착 윈도(배제 규칙 앞단 — 도착은 성공이 아니라서),
  get_unit_rates().
- `sorts.yaml.tmpl`: capacity 블록(w_eq/c_eq/set_share, 전 값 출처 주석).
- `run_all.py`: %CAPACITY_CHECK% 렌더·arm 기록, 스파이크 주입(spike_* 키).
- 백업 `*.20260810-pre-taskB.bak` 5종. 배포 md5 일치. ISSUES: I-13↔I-6
  상호 참조 + 프로빙 층 분리 명시(지시 §0 두 건).

## 8. 실패·SKIP·SUSPECT

- **SUSPECT/FAILED 0. 최종 미완 런 0** (21/21 DONE).
- s0 에서 SKIPPED 6건(재실행으로 전부 회수, 데이터 무손실): 원인은 **내
  절차 실수 2건** — ① s0 배치 실행 중 sorts.yaml.tmpl 편집(구 러너가 새
  토큰 미치환 → 렌더 검증이 차단), ② 코드 편집 후 .43 배포 지연(check_
  deploy 가 차단). 두 방어선이 설계대로 작동해 피해는 시간 ~25분. 교훈을
  PROGRESS 에 기록: 배치 실행 중 렌더 소스·배포 대상 파일 편집 금지.

## 9. 검증하지 못한 채 남긴 것

- **엣지 포화 창의 개선은 미달성** (검증 ① — 용량 항의 관할 밖임을 규명
  했을 뿐, 6~9 % 로 내리는 설계는 미구현·미검증).
- C(S3) 상한 미측정(하한 832 사용). set_share 의 S1 포함 집합
  ({S1,S2} 등)은 유추값 그대로 — 이번 검증 조건에서 미사용.
- 코호트↔IP 오름차순 매핑은 관례 의존(비대칭 부하 조건에서 라벨 뒤집힘
  시 유닛 귀속 오류 가능 — 대칭 조건에선 무해).
- w 유효 범위 = 측정 조성 볼록포(기존 한계 유지). 폐루프 생성기의 도착률
  2차 되먹임(backlog 시 도착 자체가 줄어 용량 검사가 관대해짐) 미정량.
- 일중 드리프트(+4.4 %p) 원인 미규명.
- strict+on 의 0.42 % vs far+on 0.14 % 차이(스파이크 중 EXPECTANT 1~2틱
  + S3 단독 적재)는 n=3 수준의 관찰로만 기록.

## 10. 다음에 결정할 것

1. **엣지 포화 창(검증 ①) 대응 설계**: EXPECTANT 시 손실 최소화 배정
   (예: 예상 초과 지연이 최소인 사이트로 이월 / S1 에 용량(0.9C)까지만
   주고 잔여를 차선 사이트로 — 두 안 모두 §3.1 의미론 확장). 이것이
   "73.3→6~9 %" 주장을 실측으로 만들 유일한 경로다.
2. **프로빙 층 착수 여부** (I-6/I-13 의 다른 층 해법 — 관측 유지로 연료
   차단). 용량 층과 독립이라 병행 가능.
3. **검증 ② 결과의 논문 위상**: "용량 항이 준안정 진동 어트랙터를
   제거한다(0/3 vs 3/3, 결정적 트리거)"를 주 결과로 올릴지 — n 확대
   (각 5+) 필요 여부.
4. C(S3) 상한·S1 포함 집합 share 실측을 어느 시점에 채울지.
