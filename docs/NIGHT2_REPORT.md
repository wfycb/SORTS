# 야간 무인 배치 2 보고 (2026-08-11 02:26 ~ 03:45)

총 8런(노드 장애 3 + 드리프트 5) 전부 DONE, **SUSPECT/FAILED/SKIPPED 0**,
배치 중단 조건 발동 0. 클린 복원 검증 완료 (렌더 기본 재배포, 컨트롤러·
loadgen·iptables 잔재 0, ogstun 클린, 디스크 171 GB). 진행 로그
`runs/night2-20260811/PROGRESS.md`, 분석 `analysis/night2/`,
`analysis/envoy_blindness/`.

## 1. 한 줄 결론 (과제별)

1. **동결**: `b3-freeze` 태그 푸시 + `docs/FREEZE.md`(대상 md5·해제 규칙)
   커밋. 이번 배치는 분석 스크립트만 추가 — 동결 위반 0.
2. **1000:1 재산출**: **원 수치 정확 재현** (reproduced=true) — 정의·데이터·
   재현 스크립트를 저장소에 커밋, README 반영. 실제 비는 **1024.6:1**.
3. **노드 장애**: 예측 적중 — **감지 완전 실패 (3/3런)**. S3 가 차단 창
   내내 허용 집합에 남았고, f_c 는 차단 +1.3 s 에 낙관 프라이어로 복귀.
   **I-6 의 강한 형태 발현** — "관측 기반 f_c 는 노드 생사를 판정하지
   못한다; 그 층은 LB active health check 담당" 서사 성립.
4. **드리프트**: **없음 (양 arm, 사전 등록 기준)** — 동결 코드에서 야간
   재현이 주간과 일치. +4.4 %p 는 코드 개정 2차 효과 가능성↑.
   **작업 I 현 설계 진행 가.**

## 2. 동결 (과제 1)

- 태그: `b3-freeze` (candidate 유지, 둘 다 원격 반영 확인).
- `docs/FREEZE.md` — 동결 6파일 md5 + `.43` 배포본 4종 md5 (코드 3종 원본
  일치; sorts.yaml 은 렌더 산출물이라 arm 의존 명시) + **변경은 명시적
  해제를 거친다** 규칙.

## 3. "1000:1" 재산출 (과제 2)

- **원 출처를 찾았다**: `presentation/factpack.md` §1 +
  `tables/p4_t5_envoy_blind.csv` + `p4_recompute.py` — 백업 때 docs 만
  뒤져서 못 찾았던 것 (presentation/ 은 백업 제외 대상이었음).
- **정의 (정본)**: N2 캘리브레이션 (search 단독, 밴드 정적 스윕, 라우팅
  고정, n=1800/밴드, status 200), **p50 기준**:
  - 접속측 e2e(`service_ms`): 정상 20 Mbps 6.980 → 극단 1.6 Mbps 29.543
    = **+22.563 ms**
  - Envoy 관측(access log 필드16, `DS_RX_BEG:DS_TX_END` µs): 4.456 → 4.478
    = **+0.022 ms** (%DURATION% 필드는 정수 ms 라 전 밴드 4.0 — 변별 불가)
  - 무선 구간 = 22.563 − 0.022 = **+22.541 ms** → 비 **1024.6 : 1**
- **재현: 정확 일치** (`analysis/envoy_blindness/reproduce_1000to1.py`,
  n2 원본 4파일 동봉, result_1000to1.json reproduced=true). 저장소 커밋 +
  README 개요에 수치·재현 경로 반영.
- 보조: phase4 본실험(혼합 부하·radio 교란·SORTS 반응) 창 비교는 평균 기준
  ~46:1 (R1_rr_radio c1 search Δcorrected +16.9 ms vs ΔEnvoy −0.37 ms 잡음)
  — 조건이 다르다(혼합 HOL·라우팅). 정본 수치는 통제 캘리브 정의로 인용.

## 4. 노드 장애 (과제 3) — I-6 발현 판정: **발현 (강한 형태)**

설계: 동결 코드(far+cap+soft+C_eff), 무제한 밴드, 800 rps, 본측정 t+120 에
S3:5000 차단 → t+240 해제, n=3.

차단 방식 (판단·근거): `.43` sudo iptables 불가(NOPASSWD 는 tb-radio2 만)
→ **수신측 `.40` 로컬** INPUT+DOCKER-USER 에 `-s 192.168.0.43 --dport 5000
-j DROP` — 지시 효과와 동일(S3:5000 만, ssh 22 무접촉, 컨테이너 무접촉),
자동 해제 타이머가 ssh 없이 로컬에서 돌아 더 강건. DROP = "무응답" 시나리오.
러너는 동결이라 무수정 — 오케스트레이터(`nodefail_orch.py`)가 배치 로그의
본측정 시각을 감시해 타이밍 주입.

| 측정 (§3.4) | 결과 (3런 일관) |
|---|---|
| 1. 감지 | **실패** — S3 허용 집합 포함률 1.0 유지, 이탈 0틱 (detect=null 3/3) |
| 2. f_c_src 전이 | 차단 +1.3 s obs→prior — value = **낙관 프라이어** (search 4.019 = 무부하 p95, last_obs 3.79 보다도 낙관) |
| 3. 재진입 | 이탈이 없어 무의미 — 더 강한 형태 (죽은 노드가 계속 "건강" 판정) |
| 4. 위반·손실 | 차단 창 위반 **99.1~99.4 %**, 직접 실패(비 200/바이트 이탈) **72.6~72.8 %** — S3 셰어(~42 %)를 넘는 전면 마비 = 커넥션 HOL (S3行 5 s 타임아웃이 같은 커넥션 후속 요청을 블로킹) |
| 5. 복구 | 해제 즉시 S3 트래픽 재개(+240 s 버킷부터), 단 post 창 위반 67.7~74.9 % — 타임아웃 백로그 드레인 여파 |

- soft/EXPECTANT 발동 0 — 프라이어 slack 이 전부 양수라 용량·손실 배분
  계층 모두 이 실패 모드를 볼 수 없다 (관할 밖 — 설계상 당연하고, 그것이
  이 실험의 요지).
- **해석 (사전 예측 §3.5 그대로)**: 관측 기반 f_c 는 응답이 있어야 쌓인다
  — 무응답 노드는 stale→낙관 프라이어의 사각. 이 층은 SORTS 가 아니라
  LB 의 active health check 가 담당해야 한다(현재 Envoy 클러스터에 active
  HC 없음 — passive 만). 계층 분리 서사와 정합. 프로빙(I-6 잔여 대응)의
  필요성을 판정할 실험이 이것이었고 — **판정: 필요** (또는 Envoy active HC
  로 대체. §10).

## 5. 드리프트 판정 (과제 4) — **드리프트 없음**

03:00:06 시작 (조건 자연 충족 — §4.1 대기 분기 미발동). 동결 코드로 B3
검증 ① 재실행, 교대 5런.

| arm | 야간 (03:00~03:43) | 주간 (B3, 00:30~01:22) | 차 | 임계 | 판정 |
|---|---|---|---|---|---|
| ceff on (n=3) | 5.686/5.911/6.044 → **5.88 ± 0.18** | 6.50 ± 0.85 | −0.62 | 1.03 | **없음** |
| ceff off (n=2) | 31.092/37.099 → 34.10 ± 3.00 | 30.59 ± 2.85 | +3.51 | 5.85 | **없음** |

- 야간 off 가 오히려 **높다** — "야간 = 유리" 시간대 가설의 추가 반증
  (B2 §G 기각과 일관).
- thermal S1 pkg 62~64 °C 전 런 일정 — 드리프트 없음 판정이라 상관 분석
  불요 조건.
- **해석 (사전 등록 그대로)**: 동결 코드에서는 재현된다 → 이전 +4.4 %p
  (야간 t2 70.5 vs 저녁 74.6)는 **그 사이의 코드 개정(작업 B 의 obs/
  sorts_ctl 확장) 2차 효과였을 가능성이 높다.** 작업 I(하룻밤 반복)는
  **현 설계로 진행해도 된다** — 시각 무작위화는 필수가 아니라 보수적
  선택지.

## 6. 내가 판단해서 결정한 것

1. **차단을 수신측 `.40` 로컬로** — `.43` sudo 불가 실측. 효과 동일 +
   자동 해제가 배치·ssh 와 독립이라 정지 조건 6 위험이 더 낮다.
2. **DROP (REJECT 아님)** — "응답하지 않는" 노드 장애 시나리오에 충실.
3. **러너 무수정 오케스트레이션** — 동결 준수. 로그 감시로 타이밍 주입,
   차단 스크립트 자체에 300 s 자동 해제 백스톱 이중화.
4. 과제 4 의 03:00 대기 분기는 과제 3 종료가 03:00:06 이라 미발동 —
   "계측을 돌리지 말고 쉬게" 조항은 적용 상황 자체가 없었음을 기록.
5. 1000:1 의 정본 정의를 **통제 캘리브(N2)** 로 확정하고, 본실험 조건의
   ~46:1 을 보조로 병기 — 조건 차이를 숨기지 않는다.

## 7. 실패·SKIP·SUSPECT

8/8 DONE, SUSPECT/FAILED/SKIPPED 0. 배치 기동 시 셸 경로 실수 1회(런 손실
없음, 재기동). 동결 위반 0, 가드 위반 0.

## 8. 차단 해제 검증 로그

6이벤트 전부 실증 (오케스트레이터 로그 `runs/night2-20260811/nf_orch.log`):
```text
런1 02:36:58 차단(curl 000) → 02:38:58 해제(curl 200)
런2 02:45:39 차단(curl 000) → 02:47:39 해제(curl 200)
런3 02:54:20 차단(curl 000) → 02:56:20 해제(curl 200)
최종 02:56:23 멱등 재해제 + 도달 200 / §3.6: iptables 잔재 0, precheck 통과,
컨테이너 72 무재시작 (03:00)
```
자동 해제 300 s 백스톱은 매회 걸었고 수동 해제가 선행해 무해하게 소진.

## 9. 검증하지 못한 채 남긴 것

- 노드 장애의 **완화책 실측 없음** — Envoy active HC 나 프로빙을 붙였을
  때의 감지 시간·피해 축소는 미실험 (이번 과제는 진단까지).
- 부분 장애(느려짐만, 응답은 있음)는 미실험 — f_c 관측이 보는 영역이라
  거동이 다를 것 (기존 server 교란 실험이 근사하나 완전 정지와는 다름).
- 드리프트 off arm 은 n=2 (지시대로) — off_2 37.1 의 arm 내 산포 원인
  미규명 (경계 내라 판정 영향 없음).
- 코드 개정 2차 효과 가설의 직접 검증(B 이전 코드로 재실행)은 미실행 —
  동결 코드 기준으로는 실용적 의미가 없어 제안하지 않음.

## 10. 다음에 내가 결정해야 할 것

1. **노드 장애 대응층 선택**: (a) Envoy 클러스터 active health check 활성화
   (설정 변경 — 동결 해제 필요, LB 계층 담당 서사와 정합) vs (b) SORTS
   프로빙(버린/무응답 사이트 소량 트래픽) vs (c) 진단만 논문에 싣고 대응은
   future work. (a)가 서사·비용 면에서 유리해 보이나 동결 해제가 걸린다.
2. **작업 I 착수** — 드리프트 없음 판정으로 현 설계 진행 가. 시각 블록
   vs 무작위화(보수적)만 선택하면 된다.
3. post-장애 백로그 드레인(해제 후 67~75 % 위반 지속)을 결과에 어떻게
   싣을지 — 복구 시간 정의(t+240 즉시 재개 vs 위반율 정상화 시점)의 선택.
4. NIGHT2 산출물의 저장소 반영 범위 (분석 스크립트·결과는 커밋 완료 예정;
   runs 선별 산출물 추가 여부).
