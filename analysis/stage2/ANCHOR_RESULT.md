# STAGE2 앵커런 판정 — 이탈, 정지·보고 (2026-08-11 23:08)

G1 회신 §2.1(b) 지시 이행. 런 `runs/stage2-anchor-20260811/s2anchor_t1000_1`
(DONE, 본 배치와 완전 동일 조건: T=1 s, warmup 60, duration 360, radio poor
2300 kbit c1 t+120~240, 800 rps). **본 배치는 기동하지 않았다.**

## 1. 판정 — P-S2-0 앵커 이탈

| 기준 | 값 | 등록 대역 | 판정 |
|---|---|---|---|
| **t_issue 기준** (판정 기준) | **0.305 s** (2dp 0.31) | 1.08~1.14 | **이탈** |
| t43_done 기준 (병기) | **−0.551 s** (램프 완료 전 감지) | (참고: 구판 재현대 +0.22~0.28) | — |
| ssh 램프 (apply_lat_s) | 0.856 s | phase4 0.858 | **동일 — 램프 변동 아님** |

램프가 동일하므로 t_issue 기준 이탈은 **감지 자체가 ~0.86 s 빨라진 것**이다
(코드·arm 쪽 귀속). 지시대로 대역 재정의는 하지 않는다.

## 2. config 등가성 대조표 (G1 §2.1-3)

| 항목 | phase4 A2/A4 (앵커 원출처와 동조건 est-on 런) | 앵커런 | 동일? |
|---|---|---|---|
| est_resp_bytes / est_f_c | true / true | true / true | 동일 |
| window_s | 2.0 | 2.0 | 동일 |
| 제어 주기 | t_ctrl_s 1.0 | ctl_period_s 1.0 | 동일 |
| 부하·교란·상시 밴드 | 800 rps·radio 2300 kbit 120~240·밴드 없음 | 좌동 | 동일 |
| tb-radio2.sh / tb-load 경로 | 동일 스크립트 (taskC 이후 무변경) | 좌동 | 동일 |
| subset_policy | (구판에 개념 부재 — 단일 선택) | **far_tier** | **상이** |
| capacity / soft / c_eff | (구판에 개념 부재) | **on** | **상이** |
| 코드 계열 | phase4 2026-08-07판 | **b3-freeze** | **상이** |

→ **"코드 개정의 신규 발현" 확정 서술은 불가** (G1 §3-4 준수): 코드 계열과
기본 arm(far_tier+capacity+soft+c_eff)이 함께 바뀌어 교락. 분리 실험은 §5.

## 3. 기전 (decisions.csv 실측)

- 교란 전 창은 **안정** — c1:search 전환 pre 2회뿐(정착 1 + 아래 감지 1),
  플립 없음. 클린 800 rps 에서 진동 없음은 드라이런과 일치.
- 감지 tick(+0.31 s after t_issue): `observed_rate` 가 **이미 2300** 을 보고
  d_acc 17.1 로 점프 → S3 slack −6.2 → 즉시 전환. 직전 tick(−0.7 s)은
  rate 공란·d_acc 0. 즉 **감지 시각 = 관측 rate 가 밴드를 보는 첫 tick**
  이고, 앵커런에서는 그 시점이 램프 완료(0.856 s) 전에 왔다.
- 구판(A1~A4)은 같은 관측이 t43_done **후** 0.22~0.28 s 에 왔다 — rate
  관측/d_acc 경로 또는 그 소비 시점이 taskA~B3 개정 구간에서 바뀌었을
  가능성. 원인 특정은 이 보고의 범위 밖(§5 제안).

## 4. during 창 참고 관측 (판정 아님 — P-S2-3 입력)

- **S2↔S1 플립 76회/120 s** — 미니 런(13회/20 s ≈ 78회/120 s 환산)과 일치.
  전환 간격 p50 2.0 s, 왕복(2전환) p50 3.0 s, S2 slack 0-교차 74회.
  pre 창 0회 — 플립은 poor 창 한정.
- during 위반: 전체 **0.234 %** / c1 search **0.742 %** (구판 A1: 2.187 /
  0.306). 전체가 ~9배 낮아진 것은 b3 arm 이 recommend/reserve 도 옮기기
  때문으로 보이며(during 분배 S1 9.6/S2 55.6/S3 34.8 %), c1 search 는 플립
  비용으로 2.4배 높다 — **T ablation 의 판정 모집단(c1 search) 선택이 이
  런에서도 유효**함을 보여준다.
- 루프: 426 ticks, gap p50/p99 1000/1001 ms, 오버런 0 %.

## 5. 원인 특정 제안 (승인 대기 — 임의 진행하지 않음)

**분리 실험 1런(~9 분)**: b3 코드 + **A2 등가 arm**(est on, strict_far,
capacity/soft/c_eff **off** — 렌더 스위치만으로 가능, 코드 무변경) 으로
동일 앵커런 1회.

- 결과가 1.08~1.14 로 복귀하면 → 이탈은 **기본 arm(far_tier/capacity 계열)**
  귀속. 현행 arm 이 2단계의 등록 arm 이므로, 앵커를 "구판 재현"이 아니라
  "현행 코드 기준선 재수립"으로 갈지 결정 필요 (원 등록은 유지·이탈 기록).
- 여전히 ~0.3 이면 → **코드 계열(rate/d_acc 경로)** 귀속. 개정 구간
  (taskA~B3) diff 조사로 진행.

선택지: (a) 분리 실험 승인 → 결과로 재보고, (b) 현행 코드·arm 기준선을
새로 실측해(T=1 s n=3) 새 앵커로 **신규 등록**하고 본 배치 진행(원 등록
유지·이탈 사실 보고서 명기), (c) 중단. 지시 없이는 진행하지 않는다.
