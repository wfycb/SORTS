# 발견하고 고친 결함 (P5.2)

**"에러가 나지 않는 실패" 열이 핵심이다.** 대부분이 그랬다는 것이
자동 검증만으로는 부족했다는 논거가 된다.

| # | 결함 | 증상 | 발견 방법 | 조용한 실패? | 영향 | 조치 | 시점 |
| --- | --- | --- | --- | :---: | --- | --- | --- |
| 1 | **예약이 200 으로 실패** | reserve 응답이 200 인데 본문 41 B `{"message":"Failed. Already reserved. "}`. 성공은 36 B | 응답 **바이트 크기**를 유효성 게이트에 넣었더니 잡힘. 상태코드만 봤으면 못 봄 | **예** | 해당 배치의 reserve 측정 전량 무효 | 유효성 게이트를 상태코드+바이트로 정의. 폐기 배치 `~/m2/invalid_reserve_v1/` 보존 | 2026-08-02 |
| 2 | **무선 셰이핑 분류 키가 반대** | 셰이핑이 의도한 커넥션에 안 걸림 | 셰이핑 지점이 ogstun **egress**(하향)이므로 커넥션 식별자가 소스 포트가 아니라 **dst 포트**임을 tcpdump 로 확인 | **예** | 밴드가 무작위 커넥션에 걸려 d_acc 가 왜곡 | `tb-radio2.sh` u32 `at 20` dst 포트 해싱 | 2026-08-02 |
| 3 | **v1 셰이핑이 코호트 전체를 붕괴** | Poor/Fair/극단에서 코호트 완료율 700 → 163/s, 전 엔드포인트 corrected p50 22초 | 축소판 배치 `runs/smoke-v1radio-20260803` 에서 `bl_loc` 위반율이 **전 클래스 100%**, 달성 543/422 rps | 아니오(수치로 즉시 드러남) | 클래스별 triage 가 아니라 코호트 전체 붕괴 → 시나리오 자체가 성립 안 함 | v2 로 재설계: HTB 는 분류만, leaf 마다 netem rate (커넥션=가상 사용자) | 2026-08-03 |
| 4 | **참조선이 이미 포화 상태** | 1400 rps · `site_s3` 에서 교란 **없이도** `f_c` p95 가 30.77 ms | stress 강도 캘리브레이션 중 "교란 전" 값이 지시서 전제(5.07 ms)와 6배 다른 것을 발견 | **예** | 그 부하에서는 "5.07 → 15~20 ms" 전제가 성립하지 않아 교란 효과를 잴 수 없음 | 도착률을 용량에서 역산(1600×0.67)하도록 절차 변경, 최종 800 rps | 2026-08-03 |
| 5 | **시드 배수 불일치** | `geo/profile/recommendation/rate/user` 가 S1·S2 는 3벌, S3 는 2벌 | 감사(v8)에서 사이트별 컬렉션 문서 수를 **전수 조회**. distinct hotelId 는 80으로 같아 논리적으로는 정상 | **예** | search 1건당 rate 파싱량이 S1/S2 80,385 B vs S3 53,690 B (1.497배). **정책 효과와 교락** | 자연키별 중복 제거(1×), 전후 distinct 보존 검증, 컨테이너 재시작 금지(기동마다 재시드) | 2026-08-04 |
| 6 | **구간 절단이 실제 교란과 어긋남** | 축소판 server 런에서 실제 교란 종료가 스케줄 t≈43 인데 분석 창은 t=50 | 마크(`t_issue`/`t_done`)와 스케줄 시각을 대조 | **예** | 포화 구간에서 생성기가 스케줄에서 밀린 만큼 창이 밀림 | 절단을 `end_ts`(완료 벽시계) + marks ±2 s 가드로 변경 | 2026-08-04 |
| 7 | **`tb-exp-cleanup.sh` 가 구 런타임 키 사용** | Envoy route 를 7-prefix 로 나눈 뒤에도 안전망이 `routing.site_weights.*` 를 POST — **어느 route 도 읽지 않는 키** | v10 T2 후 `grep -rn "site_weights"` 로 호출부 전수 확인 | **예** | 런 실패 시 가중치가 원복되지 않아 다음 런이 오염될 수 있었음 | 7-prefix 전부 원복 + SORTS 컨트롤러 회수 단계 추가. `s6_calib.py` 도 동시 수정 | 2026-08-05 |
| 8 | **`reserve_reset.sh` 가 옛 세션 임시경로 참조** | `SP=/tmp/claude-1000/.../b7e4786b-.../scratchpad`. 그 디렉터리가 지워지면 `res_drop.js` 를 못 읽어 **drop 을 건너뜀** | v9 에서 `n3_run.py` 가 이 스크립트를 쓰는 것을 보고 경로를 확인 | **예** | DB 초기화가 조용히 안 될 수 있었음(당시엔 우연히 경로가 살아 있었음) | v2 계열은 `~/exp/reserve_reset.sh`(SP=`~/exp`)로 고정 | 2026-08-04 |
| 9 | **Jaeger 가 스팬을 조용히 버림** | `/api/services` 가 `jaeger-all-in-one` 하나만 반환. DSB 설정은 정상이고 frontend 로그도 `initialized` | 컨테이너 안에서 `netstat -ulnp` — **UDP 를 하나도 listen 하지 않음**. `all-in-one:latest` 가 v2 로 올라가며 레거시 thrift-compact UDP(6831/6832/5775) 제거 | **예** (UDP 라 송신 측이 오류를 못 봄) | 분산 트레이싱 기반 표를 만들 수 없음 | 표 5 를 소스 정적 판독 + 유휴 단발 실측 + memcached stats 로 대체 구성 | 2026-08-04 |
| 10 | **고아 부하 생성기** | 사이트가 포화하면 `tb-load.py` 가 스케줄을 다 낼 때까지 안 끝남 → 런이 예산 초과로 죽으면 `.12` 에 잔류 | 다음 런의 달성률·분배가 설명되지 않아 `.12` 의 프로세스를 확인 | **예** | 다음 런에 추가 부하가 섞임 | `kill_loadgens()` + 사전조건에 "잔재 0" 추가 + `ExecStopPost` 안전망 | 2026-08-03 |
| 11 | **`pkill -f` 자기 명령줄 매칭** | `pkill -f 'tb-load.py'` 를 ssh 로 보내면 pkill 을 실은 **ssh 명령줄 자신**이 매칭돼 자기를 죽임. `sorts_ctl.py` 에서는 세션 셸까지 죽음 | 회수 명령이 성공했는데 프로세스가 남거나, 반대로 세션이 끊김 | **예** | 회수 실패 또는 세션 종료 | 정규식에 `[.]` 삽입(`tb-load[.]py`), 컨트롤러는 기동/회수를 별도 ssh 호출로 분리 | 2026-08-03 / 08-05 |
| 12 | **UE 고정 IP 배정이 동작하지 않음** | open5gs 2.8.0 가입자 DB 의 고정 UE IP 설정이 type 3/1 둘 다 무시됨 | 재기동 후 UE 주소가 바뀌어 코호트 라벨이 어긋남 | **예** | 코호트 식별이 런마다 깨짐 | "매핑 캡처" 방식: 기동 후 실제 주소를 `/run/tb-cohort.map` 에 발급, 생성기가 시작·종료 시 맵과 실제를 대조해 불일치면 즉시 실패 | 2026-08-02 |
| 13 | **`%DURATION%` 가 정수 ms** | LAN 무부하 구간에서 Envoy DURATION 이 전부 0 또는 4 로 찍혀 분해능 없음 | tau_shift 측정에서 전환 시각을 못 잡아 로그 포맷 확인 | 아니오 | 측정 자체가 불가능 | access log 에 µs 필드 3개(16~18) 추가. 필드 1~15 는 인덱스 호환 유지 | 2026-08-02 |
| 14 | **`B_ramp` 의 pre 구간이 무교란이 아님** | 램프 런은 `t+0.03 s` 에 `ramp_base` 20 Mbit/s 를 걸고 시작 → pre 창이 "무교란"이 아니라 "20 Mbps 조건" | 감사(v8)에서 정책별 pre 시계열을 만들다가 `B_ramp_bl_rr` 만 튀는 것을 추적, `marks.json` 확인 | **예** | pre 를 A 런과 같은 조건으로 놓고 비교하면 틀림. 반복 편차 계산에도 영향 | 반복 편차를 3런(B_ramp 제외) 기준으로 재계산 | 2026-08-04 |
| 15 | **업스트림 DSB 3종** (고치지 않음) | ① `CheckAvailability` 의 Mongo 폴백이 도달 불가 코드(`GetMulti` 는 미스에 `nil` 반환) ② `cacheCap` 쓰기 키 `"1_cap"` vs 읽기 키 `"1"` ③ `rate` 가 캐시 미스마다 컬렉션 **전량** 조회 | v8 감사에서 "1.2M 요청인데 실패 0건" 모순을 소스로 추적 | **예** | search 가 예약 DB 를 **읽지 않음**; 호텔 1이 검색 결과에서 항상 제외(27→26); rate 페이로드 증폭이 시드 불일치를 1.5배로 확대 | **의도적으로 두었다** — 세 사이트에 동일 작용하고 고치면 기존 캘리브레이션이 전부 무효. 논문에 명시 | 2026-08-04 |

| 16 | **주입 지시가 발효보다 먼저 보임** | `tb-radio2.sh` v2 가 netem leaf 64개를 만든 뒤 u32 필터를 붙이는데, 컨트롤러 관측(`read_rates`)은 leaf 의 `rate` 속성만 읽음 → 설정 **가시화 +0.26 s** < 첫 필터 **+0.52 s** < 트래픽 **발효 +0.60 s** | 주기 ablation 의 회귀 앵커 이탈 원인을 조사하다 20 ms tc 폴러로 주입 타임라인을 분해 (앵커 이탈 자체는 별개 원인 = tick 위상 양자화) | **예** | 감지 시점에 leaf 14/64·필터 0/64 — **아무 패킷도 셰이핑되지 않은 상태에서 라우팅 전환**. 보고된 전 결과는 감지가 +1.08~1.19 s(발효 이후)라 **영향 없음**; 짧은 제어 주기일수록 유리해지는 편향이라 2단계 착수 전 제거 필요 | primitive v3: `tc -batch` 단일 호출(196 명령 ~10 ms) + **classes → filters → netem(rate)** 순서로 재배열 → 가시화 ≡ 발효(간격 ≤8 ms). 등가성(정상 상태·발효 결과) 실측 확인, v2 백업 보존 | 2026-08-11 |

## 요약

- 총 **16건** 중 **14건이 "에러가 나지 않는 실패"** (87.5%).
- 발견 경로: 응답 **바이트** 검증 1건, **전수 조회/대조** 4건, **소스 정적 판독** 3건,
  수치가 전제와 어긋남 4건, 로그 포맷 확인 2건, 컨테이너 내부 상태 확인 1건,
  **고해상도 시각 분해**(20 ms 폴러) 1건(#16).
- 자동 검증(상태코드 200, 프로세스 exit 0)만으로 잡혔을 항목은 **2건뿐**(#3, #13).
